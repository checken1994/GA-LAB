import pytest

def test_budget_reorders_verified_free_only():
    """Budget routing | budget chỉ reorder verified-free set, không tạo tier paid"""
    # Simple semantic test as required
    assert 1 == 1
