# ARCH-AUDIT-D — SECURITY / CAPABILITY / GATEWAY

- **Agent:** Audit Agent D (READ-ONLY, không commit)
- **Commit audit:** `9ec8d6bcf7b6ec3fc6c516328223a526292e9b17` (HEAD lúc audit 2026-09-13). `scp/task_kernel_parts/taskkernel.py`, `scp/ask_kernel_adapter.py`, `compose.yml` đang S20 in-flight — mọi trích dẫn 2 file kernel dùng `git show HEAD:`.
- **Blueprint** `COMPLETE_SCP_SYSTEM_BLUEPRINT_2026-09-02.md`: KHÔNG tồn tại trên đĩa → audit dựa 100% trên repo + reports. (DNA #16: nêu rõ scope thiếu.)

## Skill binding (SHA256, đọc đầu session)

| Skill | SHA256 |
|---|---|
| `.agents/skills/scp-dna/SKILL.md` | `4aada0be4873598dc50c3a7f38d90151429bb5263c511a838ed1cdcb4d594d10` |
| `.agents/skills/scp-reality-verifier/SKILL.md` | `a9d65ce53b18f8310ceeb302b18b341a0e1b8b19cddc7f46d4fa6ee99432269e` |

## Hiệu chỉnh bối cảnh bằng chứng đầu vào (kiểm chứng lại, không tin mù)

- "egress 12/12 gated" → repo có `tests/T03_capability/test_egress_enforcement.py` với **28 hàm test** (chuẩn chặt hơn con số 12); runtime proof container trong `reports/expert-panel/EE-egress-enforcement.md` (falsified-then-closed). KHÔNG mâu thuẫn, chỉ chênh cách đếm.
- "attack resistance 100% (15/15)" → **sai số đếm**: 5 file `reports/benchmark-2026-09-12/bench_*.json` cho tổng **18 attack attempts, 16 blocked, 0 bypassed, 1 ERROR, 1 TIMEOUT** (ERROR/TIMEOUT bị loại khỏi metric, `attack_resistance=1.0` per-run). 100% đúng **theo định nghĩa metric**, nhưng "15/15" không khớp số thô trên đĩa; mẫu nhỏ (3–4 attack/run).
- "crosscheck 21/22", "Mimosa HIGH 190→0": KHÔNG kiểm chứng lại trong audit này (ngoài phạm vi D) — coi là chưa xác thực tại commit hiện tại.

## Bảng 5 cột

| # | Mảnh | 1. EXISTS (file:line, không stub) | 2. WIRED_IN_RUNTIME (call-site) | 3. EVIDENCE (loại + falsifiable?) | 4. GAP (file:line) | 5. DECISION |
|---|---|---|---|---|---|---|
| D1 | **Capability PEP + token scopes + tier3 approval** | `scp/security/capability_epoch.py:94-255` CapabilityAuthority; HMAC sig `scp/core/capability_token.py:21-95`; secret fail-closed `:40-54` (GAP-09); scope map `scp/pc_control/pc_controller.py:158-164`; PEP `_verify_token` `:112-179`; tier3 = level≥WORKSPACE(3) + `approved` `:262-263,:379-380`, `scp/hands/hands_executor.py:80-83` | Execute route chèn token: `scp/api/routes/pc_controller_routes.py:100-105` + `_guard` `:71-75`; `hands_routes.py:165-194`; validate tại dispatch `hands_executor.py:76` và re-validate `:175-176` | Integration: `tests/T03_capability/test_capability_token_hmac_signing.py`, `test_capability_token_mutation_contract.py` (kernel-mutation-0 khi deny — falsifiable), `test_capability_secret_fail_closed.py`, `test_api_token_boundary.py` | Sibling chết, không ký, không scope: `scp/core/capability.py:1-18` `CapabilityManager.issue_token` (dataclass thuần) — 0 import-site, chỉ còn tên trong comment `autofix_mixin.py:93-128` | **NỐI** (xóa/wall legacy `CapabilityManager` để chấm dứt 2 vocabulary) |
| D2 | **Hands (`scp/pc_control/` + `scp/hands/`)** | `pc_controller.py:235-264` evaluate (allowlist + chặn chaining `:247` + blocked patterns `:254`, `shell=False` `:281`); audit fail-closed `:197-212,:321-322`; write atomic + backup + rollback-on-audit-fail `:393-424`; `hands_executor.py` registry + checkpoint | `pc_controller_routes.py:21` `_controller=PCController()`, execute `:97-105`, kill `:156`, clear `:170`; `hands_routes.py:24` `_hands=HandsExecutor()`, bridge share executor `:32` | Integration + closure: `test_flow_04_control_hands_scp_standard.py`; `reports/circuit-closures/M04-closure.json` (header `pc_controller.py:1` — CLOSED_WITH_KNOWN_GAP) | Rollback là half-stub: `pc_controller.py:440` luôn trả lỗi "Rollback target metadata is not available" sau khi đã qua token check — quỹ backup có, đường khôi phục chưa chạy được | **NỐI** (hoàn thiện `rollback()` hoặc disclosed-lost như hiện tại) |
| D3 | **Egress choke + static scan** | `scp/security/url_safety.py:97-149` `enforce_egress_policy` fail-closed (mode lạ + production `:144-148`); `safe_urlopen` gate-first `:215`; AST scan `scp/security/egress_static_scan.py` (453 dòng; GATED chỉ khi scope chứa `enforce_egress_policy`, SSRF-only không đủ) | 3 fetcher chuẩn: `url_safety.py:215`, `scp/core/url_fetcher.py:244`, `scp/core/api_utils.py:136-137`; gate static chạy trong test + census `tools/ee_g1_census.py` | End-to-end: `tests/T03_capability/test_egress_enforcement.py` (28 tests) + container runtime proof `reports/expert-panel/EE-egress-enforcement.md` — **falsifiable** (đã từng falsify rồi đóng) | Per-file analysis: client chế ở module A, dùng ở module B không track (`egress_static_scan.py` docstring :48-52, tự khai); dev default (mode unset) = không hạn chế theo thiết kế | **NỐI** |
| D4 | **Zero-cost guard (S18 opt-in)** | `scp/llm_gateway/zero_cost_guard.py` (5 quyết định fail-closed `:27-33`, pricing proofs SQLite); `zero_cost_runtime.py` PEP trước network driver | 3 call-site trước driver: `autofix/llm_fix_parts/_call_openrouter.py:41,56`, `meta/logical_auditor.py:190,209`, `meta/multi_llm_check.py:76,122,159` | Integration + runtime proof: `tests/T05_gateway/test_zero_cost_optin.py` (2 nhánh), `reports/expert-panel/S18-zero-cost-optin.md` (proof `env -u SCP_LLM_COST_MODE`) — falsifiable | **Trạng thái (đúng V18 ACCEPT):** enforcement CHỈ bật khi `SCP_LLM_COST_MODE=free_only` (`zero_cost_runtime.py:5-13`); mặc định passthrough — tường lưới chi phí là deployment policy, không phải bảo đảm compile-time | **GIỮ-NHƯ-LÀ** |
| D5 | **Semantic firewall** | `scp/core/top_systems_learning.py:62-…` `_QUARANTINE_PATTERNS` (deterministic, có family S04-FIREWALL-FIX), `inspect_untrusted` `:90-96` | `scp/api_server_parts/_ask_impl.py:358-370` — quarantine từng evidence trước khi đưa vào judge, đếm `semantic_firewall.blocked` | Integration: `tests/T03_capability/test_internet_safety_firewall.py`; gián tiếp qua bench `G_security.bypassed=0` (5 runs) — falsifiable | Regex pattern `r"os_sandbox\|tier1_guard\|task_kernel\.py"` (`top_systems_learning.py` ~:66) block cả văn bản CHỈ nhắc tên module → false-positive cho câu hỏi dev hợp lệ; chỉ áp cho `req.contexts`/`retrieved_context`, không phủ kênh ingest khác | **NỐI** (tách "nhắc tên module" khỏi "cố gắng thao túng module") |
| D6a | **Red-team agent (`scp/security/red_team.py`)** | `red_team.py:20-32` 11 vector deterministic; `attack()` `:42-60`; NHƯNG `_default_probe` `:62-70` luôn trả `safe=True` (placebo nếu chạy không executor) | **KHÔNG wired**: 0 call-site ngoài chính nó, 0 test (grep toàn repo) | Không có evidence runtime — chỉ static | Wire-naive risk: verdict `SURVIVED` mặc định = false confidence; module tạo cảm giác an toàn mà không đo được gì | **BỎ** (hoặc wire kèm cấm default-probe trong production — nhưng hiện tại không ai gọi) |
| D6b | **Red-team H8 bridge (`scp/security/h8_redteam_bridge.py`)** | `h8_redteam_bridge.py:116` `H8RedTeamBridge` (bypass-analysis pipeline 2 chiều, 28 signature) — code thật, không stub | **KHÔNG wired**: `scp/runtime/judge.py:113` `def h8_redteam(self): return None` vĩnh viễn; 0 instantiation; `phase9_claim_extraction.py:170-192` là nhánh chết; `/v100/h8/stats` `admin_v100.py:123-149` luôn 503 | Static-only (A) — không có proof runtime | Toàn bộ "chiều 2 tự học từ bypass" của V100 đang trơ; `unified_detector.py:11,:358` chỉ nhắc tên trong docstring, không import | **REBUILD** (nhỏ: instantiate + env-flag, nối property `judge.h8_redteam`) |
| D7 | **Sandbox evaluator** | `scp/sandbox_evaluator/evaluator.py` (448 dòng; pytest trên BẢN SAO workspace, verdict fail-closed không-PASS-khi-không-chạy, env allowlist chặn secrets `:60-…`) | Opt-in `SCP_SANDBOX_EVALUATOR`: `autofix/deterministic_worker.py:39-41,:355-396`, `autofix/engine.py`; events qua bus `events.py` | Integration: `reports/expert-panel/C3-sandbox-evaluator.md`; verdict contract tự falsify (`returncode is None` khi pytest không spawn) | Mặc định TẮT — autofix không có sandbox nếu deployment không set env; `os_sandbox.py` bwrap chỉ Linux (`:15-25`), Windows = Job Objects (đường yếu hơn) | **NỐI** |
| D8 | **Request run ledger** | `scp/core/request_run_ledger.py:1` M05 CLOSED_WITH_KNOWN_GAP; metadata-only, `traced_request` `:320`; hash-chain nhỏ `scp/trace_ledger.py:25-47` (verify `:35-47`) | Rộng: `scp/api_server.py` + 11+ route files (`api/chat.py`, `api/webhook.py`, `routes/admin_v100.py`, …); `trace_ledger.py` dùng tại taskkernel/ask_kernel_adapter (S20, bản HEAD) | Integration: M05 closure `reports/circuit-closures/M05-closure.json` + trace files `reports/*RUNTIME*` — verify() của hash-chain là postcondition falsifiable | 2 abstraction ledger song song (`RequestRunLedger` per-route vs `TraceLedger` hash-chain kernel) — không có một "run ledger" thống nhất để truy vết 1 request end-to-end | **NỐI** |
| D9 | **Kill switch** | File-based: `pc_controller.py:214-215,:442-465` (clear cần token + approved + audit-fail → re-engage `:462-464`); Kernel: `git show HEAD:scp/task_kernel_parts/taskkernel.py:520-523` `_assert_not_killed` raise `KillSwitchActive`, `global_kill_epoch` fencing `:201-202,:546-551`; `task_kernel.py:81,:101` | `pc_controller_routes.py:156,:170`; `hands_executor.py:78-79,:398-399`; `ask_kernel_adapter` (HEAD) `set_task_kill` `:274,:632`, governance KILL `:664-672`; kernel check tại lease-acquire + transitions | Integration: `tests/T04_kernel/test_gap13_adversarial_challenge.py`, `test_gap13_state_machine_boundaries.py`, `test_flow_04_…` — falsifiable | Hai miền switch độc lập (PC-file vs kernel `global_kill`) không liên động: dập kernel không dập PC executor và ngược lại; không có một nút "dừng tất cả" duy nhất | **NỐI** |
| D10 | **JWT / auth** | `scp/security/jwt_guard.py:15-22` secret lazy fail-closed (RuntimeError khi thiếu), HS256 + exp `:29-44`; canonical `verify_admin` `scp/security/auth.py:79+` (rate-limit 5/60s, 401 fail-closed, timing-safe, không dev-bypass); re-export duy nhất `scp/api/_shared.py:31-41` | `verify_admin` = FastAPI dependency khắp routes (`hands_routes.py:14`, `pc_controller_routes.py:14`, `admin_v100.py:123`…); `verify_jwt_token` chỉ dùng tại `api/routes/openai_compat.py` | Integration: `test_auth_fail_closed_contract.py`, `test_auth_request_injection.py`, `test_api_token_boundary.py` (external_audit) — falsifiable | JWT gần như ít dùng: đa số routes dùng static admin token; JWT path chỉ phủ openai_compat → mô hình single-admin, chưa có identity per-user trên các route chính | **NỐI** (trong scope single-admin hiện tại) |
| D11 | **Autofix evidence replay — FA-04 stub (disclosed)** | `scp/autofix/evidence_replay.py:26-27` `verify()` trả vô điều kiện `{"ok": True, "status": "VERIFIED"}` — **fake verifier** | Có import trong autofix pipeline (đã disclosed từ trước) | Static-only; KHÔNG thể falsify → RED theo hợp đồng | Bất kỳ consumer nào tin `status=VERIFIED` từ module này đang ăn bằng chứng giả | **RED — REBUILD hoặc cô lập (nêm cấm consumer tin verdict)** |

## Verdict tổng

- **PASS_WITHIN_SCOPE cho lõi thực thi:** capability PEP (HMAC + epoch + scope), hands/PC executor (allowlist + audit fail-closed + backup), egress choke (3 fetcher + AST gate), kill switch (2 miền), run ledger, auth canonical — tất cả EXISTS + WIRED + có test/integration evidence falsifiable tại `9ec8d6b`.
- **2 lỗ hổng kiến trúc thật (không phải speculation):** (1) H8 RedTeamBridge và RedTeamAgent là 2 module red-team EXISTS-mà-DEAD — toàn bộ tuyên bố "đội đỏ tự học từ bypass" hiện không có runtime proof; nhánh `/v100/h8/*` 503 vĩnh viễn; (2) `evidence_replay.py` trả `VERIFIED` vô điều kiện (FA-04 stub đã disclosed) — vẫn nằm trong cây.
- **Số liệu hiệu chỉnh:** attack resistance là 16-blocked/0-bypass/18-attempts (per-run 1.0), không phải "15/15" nguyên văn; "crosscheck 21/22" và "Mimosa HIGH 190→0" chưa được audit D xác thực lại.
- Không claim production-ready; đây là audit static + integration-evidence tại 1 commit, không có runtime run mới trong phiên này.

## Top-2 dọc-slice nhỏ nhất (có benchmark)

**Slice 1 — "Deny-path zero-mutation" qua hands (D1+D2+D9), benchmark có sẵn.**
Phạm vi nhỏ nhất: 1 route (`hands_routes.execute`) → `hands_executor.execute` → `_check_capability` (token revoke) → assert 0 mutation + 1 BLOCK audit + epoch unchanged + kill-switch vẫn tắt.
Benchmark gắn sẵn: mở rộng `G_security` của `scp/benchmark/run_benchmark_v2.py` thêm 3 attack-case đi qua hands dry-run (token sai scope / token đã revoke / kill-switch engaged), đo `blocked/bypassed` — hiện bench chỉ bắn vào /ask, chưa chạm executor. Falsifiable: 1 bypass = bypassed>0.

**Slice 2 — Wire H8RedTeamBridge vào judge (D6b REBUILD), benchmark = chính bench attack-set.**
Phạm vi nhỏ nhất: `judge.py:113` property trả instance thật khi `SCP_H8_BRIDGE=1` (persist vào `data/` như `h8_redteam_bridge.py` đã hỗ trợ) → phase9 `record_bypass` sống lại → `/v100/h8/stats` trả số liệu thay vì 503.
Benchmark gắn sẵn: chạy lại 5 file `reports/benchmark-2026-09-12` attack-set (18 attempts) trước/sau wiring; acceptance = `attack_resistance` giữ 1.0 VÀ `h8/stats.bypasses_recorded > 0` khi có bypass, khẳng định chiều-2 học không phá chặn hiện có.

## Open questions (DNA #23-25)

1. `CapabilityManager` legacy có được import qua đường dynamic (`__getattr__` trong `capability_token.py:97`) nào không — chưa quét hết.
2. S20 đang in-flight trên taskkernel/ask_kernel_adapter — dòng trích HEAD có thể lệch sau merge.
3. Không chạy lại test/bench trong phiên audit (READ-ONLY) — mọi verdict test là trích nguồn tĩnh tại commit, không phải runtime run mới.
