# ARCH-AUDIT-A — Nhóm KERNEL/RUNTIME (Audit Agent A)

## Skill binding (SHA256)

| Skill | SHA256 |
|---|---|
| `.agents/skills/scp-dna/SKILL.md` | `4aada0be4873598dc50c3a7f38d90151429bb5263c511a838ed1cdcb4d594d10` |
| `.agents/skills/scp-reality-verifier/SKILL.md` | `a9d65ce53b18f8310ceeb302b18b341a0e1b8b19cddc7f46d4fa6ee99432269e` |

## Phạm vi & phương pháp

- **Audit theo COMMITTED STATE**: HEAD = `9ec8d6b` ("first live-LLM baseline seed99 … GA B11 handoff"). Nội dung `scp/task_kernel_parts/taskkernel.py` (1718 dòng @HEAD) và `scp/ask_kernel_adapter.py` (718 dòng @HEAD) đọc qua `git show HEAD:<path>` vì working tree đang có **S20 in-flight** (lease heartbeat: `renew_lease` +98 dòng trong taskkernel, +103 dòng trong adapter, thêm `tests/T04_kernel/test_lease_heartbeat.py` có anti-placebo control).
- Blueprint `C:\Users\check\Downloads\COMPLETE_SCP_SYSTEM_BLUEPRINT_2026-09-02.md` **KHÔNG đọc được** (file không tồn tại trên máy). Định nghĩa "mảnh" lấy từ đề bài + nội dung repo. Đây là limitation ghi nhận, không phải bằng chứng thiếu.
- Bằng chứng live do audit agent tự chạy (READ-ONLY trên repo; basetemp ngoài repo):
  - `pytest tests/T04_kernel/{test_lease_fencing_idempotency,test_transition_lease_fencing,test_kernel_storage,test_rebuild_projection_occ}` → **31 passed** (2.1s) @ worktree≈HEAD cho 4 file chưa sửa.
  - `pytest tests/T04_kernel/{test_kernel_crash_consistency,test_task_kernel_mutation_contract,test_kernel_p1_regressions,test_adversarial_kernel_flaws,test_ask_kernel_lifecycle_and_identity,test_ask_kernel_terminal_race}` → **69 passed** (11.6s).
  - `pytest tests/T04_kernel/{test_pg_storage_parity,test_pg_event_bus,test_pg_boot_runtime}` → **10 passed, 16 skipped** — skip là declared infra-skip (thiếu `SCP_PG_TEST_DSN`), KHÔNG phải skip giấu assertion; translator/factory unit test vẫn chạy không cần PG.
- Verdict scale: `NỐI` = giữ và nối tiếp; `GIỮ-NHƯ-LÀ` = đủ dùng, không đụng; `REBUILD` = sai bản chất; `BỎ` = dead code.

---

## Bảng 5 cột — các mảnh nhóm KERNEL/RUNTIME

Mảnh | EXISTS | WIRED_IN_RUNTIME | EVIDENCE (loại + falsifiable?) | GAP | DECISION
---|---|---|---|---|---
**1. Task state machine (17 states + authority)** | `scp/task_kernel.py:16` `STATES` (17 states), `:23` `ALLOWED_TRANSITIONS`; impl `transition()` `task_kernel_parts/taskkernel.py:381-518` (HEAD) — chặn terminal immutable, chặn COMPLETED/FAILED trực tiếp, WHY-Gate fail-closed cho 5 trạng thái trọng yếu, Lease Authority Gate (INV-01) | `/ask` route `scp/api_server.py:496-523` → `adapter.run_rag` → `begin()` gọi create/transition/claim/start (`ask_kernel_adapter.py:203-207` @HEAD); hands bridge `scp/hands/task_kernel_bridge.py:393-401`; retry worker `api_server_parts/lifespan.py:371-378` | Unit T04: mutation-contract + lifecycle + gap13-boundaries, **31+69 passed** do agent tự chạy @HEAD. Falsifiable: test assert `pytest.raises(InvalidTransition)` trên cạnh cấm, sửa map → đỏ. | State `WAITING_APPROVAL` nằm trong `ALLOWED_TRANSITIONS` (`task_kernel.py:26`) nhưng **không có trong `STATES`** — ghost state được `transition()` (`taskkernel.py:392` HEAD) white-list riêng; chạy đúng nhưng là mối_Collections nối miệng dễ gãy khi refactor. | NỐI
**2. Journal hash-chain (event sourcing authority)** | `_append_event` `taskkernel.py:344-360` (seq monotonic + `prev_event_hash` + `event_hash=stable_hash(body)`), `verify_journal` `:1638-1657`, `verify_integrity` `:1536-1549`, `rebuild_projection` `:1659-1708` (fail-closed khi chain hỏng) | Gọi trong mọi write-path của kernel qua `_append_event` (đường `/ask` + hands + recovery); `recover_on_boot` verify chain trước khi replay (`:1591-1594`); doubt-cron chạy `verify_integrity` định kỳ (`lifespan.py:293-300` → `core/doubt_cron.py:60-70`) | Unit `test_kernel_crash_consistency.py:49-67`: **tamper trực tiếp DB** (`UPDATE events SET payload_json=...`) → assert `hash_chain_valid is False`. Mutation-style thật: gỡ verify → đỏ. + crash-between-event-and-projection repair test. | Chưa có test tamper cấp file (bit-rot DB ngoài SQLite API) — `quick_check` không bắt tamper nội dung row; chỉ hash-chain bắt. Đủ cho single-node hiện tại. | NỐI
**3. Lease + fencing token** | `Lease` dataclass `task_kernel.py:95-102`; `claim` `taskkernel.py:526-554`, `claim_next` fair-share `:556-596`, `_assert_lease` `:609-627` (check released/expired/**fencing_token == MAX**/kill_epoch), `expire_leases` `:703-733`, `release` `:735-780` OCC | `/ask`: `claim(ttl=60)` + `start` (`ask_kernel_adapter.py:206-207` @HEAD); hands: `claim/start/heartbeat/release` (`task_kernel_bridge.py:395-401,289,629`); watchdog `api/background_jobs.py:247-261` (`kernel_lease_expiry` interval 30s, **required=True**, fail boot nếu không start) | Unit `test_lease_fencing_idempotency.py` (31 passed): stale lease bị từ chối claim/complete idempotency; `test_transition_lease_fencing`. Falsifiable: bỏ check fencing trong `_assert_lease` → test `pytest.raises(StaleLease)` đỏ. Runtime proof hands bridge `reports/runtime_proof_20260826_hands_bridge.json` (crash→UNKNOWN→recovery observed, commit `46187e7`). | TTL `/ask` cố định 60s (mảnh 4). | NỐI
**4. Lease heartbeat / renew trong /ask** | `heartbeat()` tồn tại đầy đủ `taskkernel.py:659-701` (OCC + fencing + state-gated) | **HANDS: có** (`task_kernel_bridge.py:270-291` loop renew TTL/3). **/ask: KHÔNG** — `begin()` claim ttl 60s rồi không renew (`ask_kernel_adapter.py:206` @HEAD). Watchdog không heartbeat (chỉ expire). S20 in-flight đang sửa (`renew_lease` + asyncio heartbeat trong `run_rag`) | Benchmark live `reports/benchmark-2026-09-12/bench_final_seed99.json` (2026-09-13): provider latency 30–260s (mean 85.6s) → lease hết hạn khi worker sống → `lifecycle_authority_lost` bỏ answer thật (**2/10 asks chết**, GA B11). Đây là runtime evidence đỏ đã được tái hiện, không phải suy diễn. | Tại HEAD: gap thật, đã đo bằng benchmark, S20 đang in-flight chưa commit — kết luận kernel không thể "đỏ" dứt điểm cho tới khi S20 commit + test (có anti-placebo: kill-switch off phải tái hiện lỗi cũ) pass. | NỐI (chờ S20 commit; không rework — hướng expiry-only renew + anti-placebo là đúng)
**5. OCC (optimistic concurrency)** | `OptimisticLockError` `task_kernel.py:41-60`; version check + `rowcount != 1` trong `transition/heartbeat/release/idempotency_*/commit_approval/commit_failed/rebuild_projection` (HEAD `taskkernel.py:410-416, 492-502, 676-696, 1241-1251, 1364-1374`…) | Mọi UPDATE path của kernel đều OCC; adapter/hands bắt `KernelError` family để route stale-lifecycle thay vì 500 (`ask_kernel_adapter.py:544-551, 568-580` @HEAD) | Unit `test_rebuild_projection_occ.py`, `test_satellite_occ_anti_placebo.py`, p1-regressions (trong 69 passed). Falsifiable: bỏ `AND version=?` → rowcount check đỏ test. | Không có test stress multi-writer song song thật ở mức kernel (crash-consistency là tuần tự); nhưng SQLite BEGIN IMMEDIATE + advisory lock PG làm window rất hẹp — chấp nhận được. | NỐI
**6. Idempotency (logical key + fenced claim/complete)** | Facade override fail-closed `scp/task_kernel.py:175-249,407-409` (`idempotency_claim`/`complete` **bắt buộc lease authority**, RETRYABLE→CLAIMED có OCC, không tự tạo row khi không lease) | `/ask`: claim ngay sau start, `claimed=False` → từ chối trùng (`ask_kernel_adapter.py:208-215` @HEAD); hands: claim `:403` + complete `:488` sau verifier; reconcile PARTIAL/CONFLICT → HUMAN_REVIEW (`task_kernel.py:255-349`) | Unit `test_lease_fencing_idempotency.py` (3 test passed): stale lease không claim được, recovery reader chỉ probe không mutate. Falsifiable: bỏ `_assert_lease` → đỏ. + reconcile outcome contract test T10. | Deadline cố định id per ask: key/session là scope-retry, hash là per-question (S19 BUG-2 fix) — logic đã đúng tại HEAD. | NỐI
**7. Kill switch** | `_assert_not_killed` `taskkernel.py:520-524`; `set_global_kill` `:1412-1423` (epoch tăng khi ON); `set_task_kill`/`cancel` `:1425-1449`; epoch bị `_assert_lease` kiểm (`:613`) → lease cấp trước epoch cũ chết ngay | `claim/claim_next/commit_approval` gọi `_assert_not_killed`; mọi `_assert_lease` check epoch+kill. Kill switch là máy-trạng-trong-DB → mọi kernel instance nhìn thấy ngay | Unit trong p1-regressions/adversarial (trong 69 passed) — kill ON → claim raise `KillSwitchActive`. Falsifiable: bỏ check → đỏ. | Không tìm thấy route HTTP operator để bật kill switch (phải gọi Python trực tiếp) — control-plane cho người vận hành mỏng, không phải lỗi kernel. | NỐI (route operator là việc nhóm khác)
**8. Recovery + checkpoint + reconcile (cổng F)** | `checkpoint` `taskkernel.py:782-817` (payload_hash + secret-scan `_assert_checkpoint_safe`), `validate_checkpoint` `:970-983`, `record_action_dispatched`→UNKNOWN `:819-866`, `enter_reconciling` `:886-911`, `reconcile_unknown` `:913-968` + PARTIAL/CONFLICT (facade `:255-349`), `recover_on_boot` `:1569-1618`, `auto_reconcile_orphans` `:1451-1534` (P1 fix: gate lease expires_at, không hijack worker sống), `recovery_decision` `:1710-1718` | `recover_on_boot` gọi trong lifespan `api_server_parts/lifespan.py:280-290` (điều kiện: DB tồn tại; fail non-fatal có log); watchdog orphan 60s **required=True** (`background_jobs.py:264-277`); hands bridge dùng UNKNOWN/reconcile đầy đủ (`task_kernel_bridge.py:217,653-655`); runtime proof chứng minh UNKNOWN→recovery flow thật | Unit: crash-consistency (repair), `test_kernel_p1_regressions` (watchdog không hijack lease sống — đã trong 69 passed), T10 reconciliation-outcome-contract. **Runtime evidence cấp D cho hands**: `runtime_proof_20260826_hands_bridge.json` — mutating dispatch bị fail giữa chừng → kernel_state UNKNOWN + `requires_recovery` đúng contract. Falsifiable: sửa logic expire-gate → p1 test đỏ. | `/ask` (R0 read-only) không dùng `record_action_dispatched` — hợp lý vì không side-effect; hands mới là consumer. | NỐI
**9. Backpressure / admission control** | `in_flight_count` `taskkernel.py:1620-1624`; cap check `ask_kernel_adapter.py:194-201` @HEAD (`SCP_ASK_MAX_INFLIGHT`, default 200, fail-closed KernelError); fair-share owner trong `claim_next` `:556-596` | `/ask begin()` check cap trước create_task; response blocked → `_kernel_blocked_response` (`ask_kernel_adapter.py:652-686` @HEAD) | Unit: chưa thấy test riêng cho cap (không có file test_backpressure). Logic 3 dòng, đọc trực tiếp là deterministic. | **GAP nhỏ**: cap không có test tự viết; kernel `claim_next` max_active_per_owner cũng chưa thấy caller runtime (adapter dùng `claim` trực tiếp, không qua queue-fair-share) → hàng đợi fair-share hiện mới có cơ chế, chưa có consumer production. | NỐI; thêm 1 unit test cho cap + quyết định có cần `claim_next` cho /ask hay không (nếu không → đánh dấu internal-use cho consumer tương lai)
**10. Request ledger** | Kernel-level: `record_action_dispatched` (provider_request_id persist trước khi tin provider — `taskkernel.py:819-866`) — **không có caller production ngoài hands**; API-level: `RequestRunLedger` `core/request_run_ledger.py:99+` (JSONL `data/request_runs.jsonl`, redact secret) | API-level wired dày: `/ask` `@traced_request(_REQUEST_RUN_LEDGER)` (`api_server.py:497`), admin_v98/v100, chat ledger (`api/chat.py:41,504`); kernel-level wired qua hands bridge `:217` | Kernel-level: unit adversarial + T10 (passed). API-level: wired static + benchmark live seed99 record run_status/ledger_status trong `bench_final_seed99.json`. Falsifiable: mock một request fail → ledger record kiểm tra được trong JSONL. | Hai tầng ledger (API JSONL vs kernel UNKNOWN-checkpoint) KHÔNG join với nhau bằng id chung trong query — trace_id có trong cả hai nhưng chưa có tool đối chiếu tự động. | NỐI
**11. Storage abstraction — SQLite** | `KernelStorage` Protocol `kernel_storage.py:32-80`; `SQLiteKernelStorage` `:83-183` (WAL, per-thread conn, BEGIN IMMEDIATE retry 25 lần, backup API online); `make_storage` `:185-238` (fail-closed env switch) | Inject mặc định cho mọi `TaskKernel(db_path)`: `/ask` adapter (`api_server.py:184-197`), lifespan recovery `:286`, retry worker `:359,373`, hands `:55` | Unit `test_kernel_storage.py` (trong 31 passed) + mọi test T04 chạy trên backend này. Falsifiable: backend违反 protocol → tức thì TypeError/AttributeError trong test. | SPOF single-node là giới hạn đã khai báo trong docstring (`kernel_storage.py:197-200`), không phải gap ẩn; HA đa node là kiến trúc khác. | GIỮ-NHƯ-LÀ
**12. Storage parity — PostgreSQL (Track C1)** | `PgKernelStorage` `kernel_storage_pg.py:263+`, translator `translate_sqlite_sql` (mask literals, fail-closed trên strftime không hỗ trợ, INSERT OR IGNORE→ON CONFLICT, qualify DO UPDATE SET), advisory xact lock = write slot, schema `kernel_storage_pg_schema.sql` | Opt-in qua env `SCP_KERNEL_BACKEND=postgres` + `SCP_KERNEL_PG_DSN` (`make_storage`, fail-closed không DSN) — **deployment compose hiện không set** → default SQLite; không có silent flip | Parity suite real-PG (same op sequence so projection/journal 2 backend) — **16 test skipped (declared infra-skip)** tại lần chạy của agent; 10 translator/factory test passed. Agent KHÔNG có DSN → parity runtime chưa được xác nhận lại trong session này. Bằng chứng trước đó: `reports/expert-panel/C1-postgres-storage.md`. | Parity cần PG thật để chứng minh lại định kỳ (CI không bật DSN); translator là lớp dễ ám ảnh silent-wrong nếu kernel thêm cú pháp SQLite mới — có fail-closed bù. | NỐI (bật DSN trong 1 job CI định kỳ, không cần code change)
**13. Event bus PG NOTIFY (Track C2)** | `PgEventBus` `event_bus_pg.py:139+`: publish row+pg_notify cùng transaction `:205`, subscribe_poll cursor+LISTEN `:239`, replay_undelivered `:407`, poison guard dead-letter `max_attempts`, `make_event_bus` opt-in fail-closed `:472`; schema `event_bus_pg_schema.sql` | Consumer: `sandbox-evaluator` compose profile "sandbox" (`compose.yml:126-135`, `SCP_EVENT_BUS_ENABLED=1`, exit 2 nếu thiếu DSN — `sandbox_evaluator/loop.py:96-118`); Publisher: `autofix/deterministic_worker.py:229-241` (lazy). **Default compose KHÔNG bật** → opt-in thật | Suite (a)-(i) real-PG gồm test then chốt "(b) listener down khi publish → replay mất 0 event" — skipped cùng 16 ở trên (declared). Falsifiable-by-design: NOTIFY-miss phải được replay cứu, test b assert chính xác điều đó. | Cùng điều kiện với mảnh 12: chưa xác nhận lại runtime trong session (không DSN). Không có subscriber trong path /ask — bus hiện chỉ phục vụ sandbox-eval loop. | NỐI (cùng job CI DSN với mảnh 12)
**14. TraceLedger (JSONL hash-chain)** | `TraceLedger` `trace_ledger.py:25-46` (seq + prev_hash + hash + redact secret, verify) | `AskKernelAdapter.__init__` tạo `data/ask_task_kernel_trace.jsonl` (`ask_kernel_adapter.py:64` @HEAD); append tại begin/finalize/fail (`:231-240, 588-603, 633-641`) | Verify logic unit test nằm trong T04/T08 (verify có kiểm secret-leak + chain). Là A-level evidence: append-only JSONL, không có cơ chế chống xóa file. | `append` đọc-ghi toàn file mỗi lần (`read_text().splitlines()` → O(n) theo số entry) — sẽ chậm khi trace lớn; chưa thấy rotation. Không đe dọa đúng đắn, đe dọa hiệu năng dài hạn. | NỐI (rotation/append-stream khi file > vài MB)

---

## Điểm cần lưu thêm (không vào bảng)

1. **Ghost state `WAITING_APPROVAL`** (`scp/task_kernel.py:26` vs `:16`): reachable qua `transition()` (được white-list riêng ở `taskkernel.py:392` HEAD), có `commit_approval` cryptographic (facade `taskkernel.py:1183-1280` HEAD) — hoạt động đúng nhưng việc không nằm trong `STATES` khiến `checkpoint(state=...)` từ chối và các vòng lặp duyệt `STATES` bỏ sót nó. Ghi nhận là debt cleanup nhỏ.
2. **S20 in-flight** (`git diff`): `renew_lease(task_id, lease_id, fencing_token, ttl)` — expiry-only, từ chối renew khi state terminal/RECOVERING; test `tests/T04_kernel/test_lease_heartbeat.py` có **anti-placebo control (test c)**: kill-switch off phải tái hiện `lifecycle_authority_lost` cũ. Thiết kế đúng DNA (không nới TTL, không phá fencing).
3. Không tìm thấy secret trong các file đã audit; payload checkpoint có secret-scan fail-closed (`taskkernel.py:119-140` HEAD).

---

## Top-2 dọc-slice nhỏ nhất (có benchmark đo được, thuộc nhóm KERNEL/RUNTIME)

### Slice 1 — "S20 lease-heartbeat e2e cho /ask" (nhỏ nhất, đang dở 1 bước)
- **Các ô phải nối**: `TaskKernel.renew_lease` (taskkernel.py, in-flight) → `AskKernelAdapter.run_rag` heartbeat asyncio (adapter, in-flight) → env kill-switch `SCP_ASK_HEARTBEAT_*` → watchdog `expire_leases` (background_jobs.py:247) không được phá lease sống.
- **Size ước lượng**: ~1 ngày agent — S20 đã code 2/3; còn lại: chạy đủ suite T04 + commit + 1 benchmark live seed-định-tôi (tái hiện seed99) xác nhận 10/10 asks không còn `lifecycle_authority_lost`.
- **Benchmark đo**: `reports/benchmark-2026-09-12/` harness (bench_final_seed99.json) — chỉ số: số ask kết thúc `COMPLETED` vs `HUMAN_REVIEW(lifecycle_authority_lost)`; baseline 8/10, target 10/10 tại cùng provider latency (30–260s).

### Slice 2 — "PG parity + event-bus replay smoke trong CI có DSN"
- **Các ô phải nối**: `make_storage(postgres)` → `PgKernelStorage` → parity suite; `make_event_bus` → `PgEventBus.publish/subscribe_poll/replay_undelivered` → `sandbox_evaluator/loop.py main()`.
- **Size ước lượng**: ~0.5–1 ngày infra — docker service postgres:16-alpine trong CI + 1 job chạy 3 file T04_pg_* với `SCP_PG_TEST_DSN` (docker hint đã nằm sẵn trong test), không sửa product code.
- **Benchmark đo**: pytest duration + assert zero-skip; số test được chuyển từ "declared skip" → "passed" (16 skipped hiện tại → 0). Kèm 1 lần đo parity sequence runtime (ms) làm baseline latency PG vs SQLite.

---

## Open questions (còn lại sau audit)

1. `claim_next` fair-share chưa có consumer production — giữ như cơ chế chờ nối, hay wiring cho /ask để có queue priority thật? (quyết định owner).
2. Không có route operator cho global kill switch — kernel API sẵn sàng nhưng control-plane HTTP chưa có; thuộc phạm vi nhóm khác nhưng ghi nhận để không ai tưởng "kill switch wired đến người vận hành".
3. Blueprint không đọc được → đối chiếu "17 states" với blueprint gốc chưa thực hiện được; count 17 xác nhận từ `STATES` (`scp/task_kernel.py:16-19`).
