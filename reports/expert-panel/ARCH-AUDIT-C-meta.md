# ARCH-AUDIT-C — META / SELF-MODEL / WORLD-STATE (Evidence-First)

- **Audit Agent:** C (kiến trúc bằng-chứng, READ-ONLY)
- **HEAD:** `9ec8d6bcf7b6ec3fc6c516328223a526292e9b17`
- **Skill binding (SHA256):**
  - `.agents/skills/scp-dna/SKILL.md` = `4aada0be4873598dc50c3a7f38d90151429bb5263c511a838ed1cdcb4d594d10`
  - `.agents/skills/scp-reality-verifier/SKILL.md` = `a9d65ce53b18f8310ceeb302b18b341a0e1b8b19cddc7f46d4fa6ee99432269e`
- **Blueprint:** `C:\Users\check\Downloads\COMPLETE_SCP_SYSTEM_BLUEPRINT_2026-09-02.md` **KHÔNG tồn tại** trên máy → so Salvador dựa trên repo docs (GA.md B11, circuit closures).
- **Phương pháp:** static call-site tracing từ entry points (lifespan, /ask, registry) + 3 live probes Python trên HEAD (probes ghi ra temp dir, không sửa repo) + đối chiếu DB local read-only + test inventory.
- **Scope hạn chế:** chưa chạy container full E2E tại HEAD; probes là unit-level reality checks, không phải C-level proof.

## Reality probes đã chạy tại HEAD (falsifiable, tái hiện được)

| # | Probe | Kết quả |
|---|---|---|
| P1 | `EntityEventAuthority.record_event(evidence_refs=[])` đúng như hook /ask truyền | **CONFIRMED FAIL** — `WorldStateError: OBSERVED assertion requires evidence_refs` → hook /ask raise MỖI lần verdict PASS |
| P1b | Cùng probe nhưng `evidence_refs=['ev-1']` | OK — assertion ghi được (cơ chế ổn, chỉ hook sai tham số) |
| P2 | `run_doubt_cycle(tempdir)` end-to-end | Chạy thật 4 checks → verdict `DOUBT_DETECTED`, `fitness_drift=False` (fail-closed đúng thiết kế) |
| P3 | `hasattr(RealityJudge(), 'schedule_background_jobs')` + MRO | **False** — MRO `[RealityJudge, object]`; judge mới không có scheduler, không inherit bất kỳ mixin nào |
| P4 | `JudgeVerdict(verdict='PASS').ctx` | **CONFIRMED AttributeError** — dataclass `judge_parts/types.py:14` không có trường `ctx` |

## Bảng 5 cột

| # | Mảnh | 1. EXISTS | 2. WIRED_IN_RUNTIME (call-site) | 3. EVIDENCE (falsifiable?) | 4. GAP | 5. DECISION |
|---|---|---|---|---|---|---|
| 1 | **WHY Engine** `scp/meta/why_engine.py:76-86` + `why_engine_parts/whyengine.py:44-507` | Có, không stub: 12 target patterns, 11 evidence strategies, 8 why_sources thật, atomic claim `UPDATE..RETURNING` (whyengine.py:361), fix 5.356-stuck = id-based UPDATE (`plan_id`, why_engine.py:34-40) | (a) /ask pre-judge hook: `_ask_impl.py:111-112` → `api/cognitive_router.py:22-29` tạo plan cho câu "tại sao/why"; (b) background 5min: `api_server_parts/lifespan.py:302-336` gọi `run_pending_verification_cycle` qua `judge.why_engine` do `helpers.py:301-308` gắn vào judge singleton | M12-closure (pinned `fa9da62`): log `[WHY-VERIFY] cycle executed=1` + row flip executed/PASS — C-level 1 cycle, KHÔNG soak; test `tests/T03_capability/test_flow_12_background_why_scp_standard.py` — có thể kill nếu atomic claim/UPDATE hỏng. Local HEAD: `data/v13.db` why_verification_plans **total=0** → INSUFFICIENT locally | Tại HEAD local không tái hiện được executed=1 (DB rỗng); WHY verdict chỉ nằm DB, không đội điểm /ask (xem #2) | **NỐI** |
| 2 | **WHY Gate v9.0 "primary control gate"** `scp/meta/why_gate.py` (650 LOC) + `judge_parts/phases/phase7_build_verdict.py:38-63` | why_gate substantive (EmoBank, TOP-1% enrichment, audit jsonl); phase7 wiring `if ctx.verdict.ctx.verdict == "PASS"` | **DEAD 2 LỚP:** (1) `RealityJudge` mới (`scp/runtime/judge.py:61`, MRO probe P3) không inherit `JudgeCoreMixin` → phase7 không chạy trong /ask thật; (2) ngay trong phase7, `ctx.verdict.ctx` = **AttributeError** (probe P4) → nuốt bởi `except → logger.debug` (phase7:62-63) | Static: 0 importer của JudgeCoreMixin ngoài chính nó; P3+P4 falsifiable, tái hiện 100% | "WHY Gate chốt PASS→UNKNOWN" là claim chết + code hỏng; `_ask_impl.py:460` đọc `evidence['why_gate']` sẽ luôn rỗng | **REBUILD** (port why_gate sang judge 2-tier) hoặc xóa claim |
| 3 | **BehaviorMonitor** `scp/meta/behavior_monitor.py:87-454` | Có: heuristic deterministic (EmoBank-style), decay, Roses/Buds/Thorns | Chỉ qua why_gate control-gate (`why_gate.py:303-325`) trong autofix paths (`autofix/runner.py:270`, `autofix_mixin.py:198`, `reflectmixin.py:212` — chạy trong deep-audit loop live, lifespan.py:197-222). KHÔNG có trong /ask verdict path (phase7 chết) | Gọi được thật (import probe gián tiếp qua P2 why-gate cycle); **0 test** tìm thấy trong tests/ | Không gate được verdict; không test → claim "control gate" chỉ đúng cho autofix | **NỐI** (giữ) + thêm test |
| 4 | **CuriosityAsker** `scp/meta/curiosity_asker.py:18-70` | Có nhưng mỏng (entity-guard + template questions, KHÔNG đọc `pending_resolutions` như unknown_state.py:141 hứa) | **0 call-site runtime** (grep toàn repo: chỉ comments ở `unknown_state.py`, `counter_question.py`, `scpv14_process_mixin.py:117`) | Docstring tuyên bố "Behavioral test test_v10439_g expects this module" — **test không tồn tại** trong tests/ (grep 0) | Dead module khôi phục cho một test không còn tồn tại; contract "CuriosityAsker reads pending_resolutions" là fictional | **GIỮ-NHƯ-LÀ** (chờ slice #S3) |
| 5 | **CuriosityEngine / MetaCognitionEngine** `scp/meta/meta.py:176, 796` (bảng `meta_curiosity`) | Có thật: `_find_gaps/contradictions/novelties/anomalies/edge_cases`, ghi `meta_curiosity` | **Chỉ CLI** `python -m scp.meta.meta` (meta.py:918-926). 0 service wiring; `pending_resolutions` không có reader nào (`archive.py` chỉ expire) | Static grep; không có runtime proof | Meta-curiosity loop (sinh câu hỏi → hỏi lại → học) không chạy 24/7 | **NỐI** qua slice dọc (S3 candidate) |
| 6 | **Self-model + Blind-Spot Registry** `scp/self_model/capability_map.py:80-275` | Có, chuẩn evidence-first: proofs immutable (trigger RAISE), blindspots OPEN/MITIGATED/CLOSED, stale-SHA rejection (capability_map.py:205-210) | Route `/v106/capabilities/{cap_id}` (`api/routes/v106_routes.py:44-66`) — recompute on-the-fly. **`tested_sha="HEAD"` HARDCODE** (v106_routes.py:66) → mọi proof gắn literal "HEAD", mất provenance thật | Tests: `tests/T06_verifier/test_self_model_capability_map.py`, `tests/test_subsystem_self_model.py` | `tested_sha="HEAD"` phá đúng contract "bound to the exact tested SHA" mà module tự đặt ra; `add_blindspot` 0 runtime writer (xem #7) | **NỐI** + sửa tested_sha = git SHA thật |
| 7 | **DoubtAuthority (Doubt Engine hình thức)** `scp/knowledge/doubt_engine.py:39-90` | Có: DoubtRecord, MissingPiece, anti-self-resolve, tự đăng ký blindspot | **Test-only** (`tests/T02_contract/test_doubt_engine.py`). 0 call-site runtime | Test T02 (falsifiable cho module) | Self-model có reader nhưng không ai viết doubt/blindspot lúc runtime → registry cứ trống | **NỐI** (wire vào doubt_cron/closure flow) |
| 8 | **Doubt Cron** `scp/core/doubt_cron.py:38-198` | Có: 4 checks (fitness/kernel-integrity/escalation-backlog/why-gate-anomaly) + `doubt_ledger.jsonl` | `lifespan.py:293-300` start (6h interval, chạy ngay vòng đầu), stop `lifespan.py:436-437` | **Probe P2 chạy thật end-to-end tại HEAD** → DOUBT_DETECTED + ledger ghi — falsifiable; tests T01 boot + T03 flow12 | Chỉ BÁO CÁO ledger + warning; không notify/escalate cho người khi DOUBT_DETECTED ngoài log | **NỐI** |
| 9 | **World-State authority** `scp/world_state/temporal_authority.py:74-160`, `entity_event_authority.py:11-72`, `world_state_projection.py` | Có, bitemporal append-only + trigger immutability + anti-self-promotion (temporal_authority.py:110-111) + OBSERVED yêu cầu evidence_refs (:91-92) | Routes (gated profile=full, `api_server.py:421-438` + `world_state_routes.py:39-164`) + /ask PASS hook `_ask_impl.py:528-543` | Tests T02 contract + **T09 golden e2e**; **P1/P1b**: hook /ask truyền `evidence_refs=[]` → raise `WorldStateError` MỖI PASS → 0 event world-state từ runtime (fail-open qua warning :544) | Hook /ask dead-on-arrival 1 dòng (thiếu evidence_refs/run_id) | **NỐI** + fix hook (Slice S2) |
| 10 | **Prediction routes + PredictiveOrchestrator** `scp/api/routes/prediction_routes.py:40-127`, `prediction/predictive.py` | Có: run-cycle/verify/stats, admin-auth, ledger traced | Engine gắn ở `helpers.py:287-295` (get_judge) → `prediction_routes.py:47-63` đọc đúng canonical owner (fix M6). Routes chỉ đăng ký khi profile ≥ full (`route_profile.py` GROUP_MINIMUM) | Static + tests flow; route tự bộc lộ 503 nếu engine None (falsifiable wiring) | Native `.env` đang `SCP_API_PROFILE=core` (`.env:11`) → routes Prediction/Calibration/Forecast/Risk/WorldState **không đăng ký** khi chạy native; compose thì `SCP_PRODUCTION_MODE="0"` → full. Hai chế độ lệch bề mặt API | **NỐI** + quyết định profile nhất quán |
| 11 | **Calibration** ledger mới `scp/calibration/ledger.py` + `routes/calibration_routes.py`; legacy `scp/meta/calibration_engine.py:93+` | Ledger có: record/resolve/accuracy per-domain; legacy engine 607 LOC | Ledger: /ask hook `_ask_impl.py:548-562` (UNKNOWN/PARTIAL/FLAGGED) + routes. **Legacy CalibrationEngine: chỉ gọi trong phase7 chết** (`phase7:188-205` `self.calibration.log_verdict`) → chấm điểm confidence theo thực tế KHÔNG chạy; `legacy_adapter` 0 caller | Tests `tests/T06_verifier/test_calibration_ledger.py` + T02 flow02 — falsifiable | Không có auto-resolve/chấm điểm định kỳ; Brier/ECE legacy chết theo phase7 | **NỐI** ledger; **REBUILD** vòng chấm điểm tự động |
| 12 | **Forecast** `scp/forecast/ledger.py` + `routes/forecast_routes.py:36-144` | Có: append-only registry + resolve với outcome codes | /ask hook keyword-trigger (`_ask_impl.py:565-586`, luôn `outcome_code=9`) + routes (profile=full) | Static; routes fail-closed 500 không leak (AUDIT MACH2-BUG4) | Resolve chỉ admin-manual; hook chỉ bắt keyword (sẽ/predict/will) | **NỐI** |
| 13 | **Risk Intelligence routes** `scp/api/routes/risk_routes.py:17-142` | Có: RiskClassifier PR0-PR5, IncidentStateMachine, AlertRouter | /ask hook `_ask_impl.py:~505-510` (classify signals mỗi request) + routes (profile=full) | Static + hook observable qua `evidence['risk_level']` | `_incidents` in-memory thôi (risk_routes.py:25) — restart mất incidents | **NỐI** (khi cần persist → nối world_state) |
| 14 | **Judge background scheduler** `judge_parts/judgebg_mixin.py:59-175` (ThreatSim 6h, IntelCrawl 12h, ReVerify 5m, Canary 10m, Storage 30m, PayloadGen 12h) | Code thật 175 LOC, nhiều fix anti-silent documented | **DEAD:** `lifespan.py:134-157` chờ `_judge` rồi gọi `asyncio.run(_judge.schedule_background_jobs())` — judge mới KHÔNG có method (probe P3: hasattr=False) → luôn rơi `except` → `logger.error('[4-a-001] Background scheduler failed')`, `background_scheduler_started=False`. `JudgeBgMixin`: 0 user | P3 falsifiable 100% tại HEAD | ThreatSim/IntelCrawler/ReVerify/Canary-scan/Storage/PayloadGen đều không chạy; ReVerify queue chỉ còn /ask-sync path xử lý | **REBUILD** (port scheduler sang RealityJudge mới hoặc đưa vào background job registry) |
| 15 | **Old judge pipeline** `judge_parts/judgecore_mixin.py` + `phases/` (WHY gate, cognitive gate, question_tracker, knowledge-save, governance, calibration log trong verdict path) | ~2.800 LOC, từng là live path V104 | 0 importer ngoài `__init__`-style wiring; `runtime/judge.py` thay thế hoàn toàn (docstring "Replaces the bloated RealityJudge") từ 2026-08-29 (Cổng D) | Static; P4 cho thấy code còn hỏng (`ctx.verdict.ctx`) → codemod refactor sai, không ai chạy nên không ai thấy | Dead code khổng lồ + broken, tạo illusion "10-phase pipeline"; shadow `judge_phases.py:13` tự nhận "claim là FICTION" | **BỎ** (xóa sau khi port các mảnh còn giá trị: why_gate #2, scheduler #14) |

**Lưu ý sticky-issue "5.356 plans stuck":** tại HEAD, root-cause cũ (`UPDATE..ORDER BY..LIMIT`) đã sửa 3 lớp: atomic `UPDATE..RETURNING` + fallback per-row claim (whyengine.py:361-370), id-based status UPDATE (why_engine.py:34-40), lazy `VerificationPlan` resolve (whyengine.py:171-172, 372-373). Container-proof executed=1 tồn tại nhưng pinned ở SHA cũ (`fa9da62`) — tại HEAD local DB rỗng nên verdict **VERIFIED-within-old-scope / INSUFFICIENT tại HEAD** → cần S1 benchmark tại HEAD.

## TOP-2 DỌC-SLICE NHỎ NHẤT CÓ BENCHMARK

### S1 — WHY loop end-to-end tại HEAD (nhỏ nhất, ~chỉ thêm harness + 1 admin trigger)
- **Đường:** `/ask` câu "Tại sao X?" (seed cố định N=10) → `cognitive_router.py:25` tạo plan → `why_verification_plans` (pending) → gọi `run_pending_verification_cycle(limit=10)` trực tiếp (trigger admin hoặc test harness — tránh chờ 180s+300s của thread) → `why_execute_plan.execute_plan` query sources → row flip `executed` + verdict + `sources_queried`.
- **Benchmark (falsifiable):** assert `executed>=N` trong DB; mỗi row có verdict ∈ {PASS,FAIL,CONFLICT,UNKNOWN}; `sources_queried` ≠ ""; đo p50/p95 thời gian/plan; rerun seed → idempotent (không double-claim — kháng thử nghiệm P-dupe 8-thread đã có sẵn). Product hỏng (claim/update/lazy-resolve) → executed=0 → benchmark FAIL.
- **Effort:** không sửa product core; chỉ benchmark + 1 endpoint/CLI trigger. Giải luôn nợ "executed=1 chỉ chứng minh ở SHA cũ".

### S2 — World-State PASS event thật (product-fix 1 dòng, benchmark observable)
- **Đường:** sửa hook `_ask_impl.py:530-543`: truyền `evidence_refs=[run_id]` (run id từ `RequestRunLedger`/`stage_request`) hoặc ghi qua đường INFERRED → hook ngừng raise (P1) → mỗi verdict PASS ghi 1 assertion append-only.
- **Benchmark:** 5 câu /ask trả PASS → `GET /v105/world/projection?subject=entity:ask_session` → count PASS events == 5; chạy lại → tăng đúng 5 (append-only, không mất history); hook cũ → count 0 → FAIL. Kèm test: `tests/T09_golden_task/test_golden_world_observation_e2e.py` mở rộng.
- **Effort:** nhỏ nhất trong các product-fix; mở đường cho S3 (blindspot writer + promotion flow đã có sẵn anti-self-promotion).

*(Candidate S3 lớn hơn — curiosity→sinh câu hỏi→/ask→calibration chấm điểm: CuriosityEngine (meta.py:176) + CalibrationLedger + resolve cron; hoãn vì cần wire mới ở 3 điểm, không còn là "slice nhỏ nhất".)*

## Verdict nhóm

Nhóm META/SELF-MODEL/WORLD-STATE **không có stub trông thấy** — nhưng đang chia đôi: tầng **live có bằng chứng** (WHY engine+loop, Doubt Cron, CalibrationLedger, Forecast/Risk hooks, background job registry: deep-audit/attack-monitor/kernel-jobs) và tầng **dead hoặc dead-on-arrival** (WHY Gate verdict-path, judge scheduler, world_state /ask hook, legacy CalibrationEngine, CuriosityAsker, toàn bộ old judge pipeline ~2.800 LOC broken-và-chết). Nghiêm nhất: 2 wiring claim "control" (WHY Gate PASS→UNKNOWN; BehaviorMonitor gate) hiện không tác động gì tới /ask thật, và judge background scheduler lỗi mỗi boot mà chỉ có log error. `PASS_WITHIN_SCOPE` của các gate cũ không thể mở rộng thành "meta-layer hoạt động" — đúng DNA #22.

## Open questions (không kết luận)
1. `SCP_API_PROFILE=core` (native) vs compose full — bề mặt API nào là deliberate? Cần owner quyết.
2. Old judge pipeline: xóa hay port? Ảnh hưởng tới test parity (`test_god_split_semantic_parity.py`).
3. `tested_sha="HEAD"` ở v106 — có phải deliberate placeholder chưa sửa?
4. Soak/uptime cho WHY loop vẫn chưa có (M12 known gap giữ nguyên).
