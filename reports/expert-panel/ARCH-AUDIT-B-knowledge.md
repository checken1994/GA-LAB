# ARCH-AUDIT-B — KNOWLEDGE / LEARNING (kiến trúc bằng-chứng, READ-ONLY)

- Agent: Audit Agent B (nhóm KNOWLEDGE/LEARNING)
- Ngày: 2026-09-13
- Snapshot: git `9ec8d6bcf7b6ec3fc6c516328223a526292e9b17` (commit 2026-09-13 16:40:45 +0700; working tree dirty: `compose.yml`, `scp/ask_kernel_adapter.py`, `scp/task_kernel_parts/taskkernel.py` modified, vài file untracked — mọi file:line dưới đây đọc từ working tree tại snapshot này)
- Blueprint `C:\Users\check\Downloads\COMPLETE_SCP_SYSTEM_BLUEPRINT_2026-09-02.md`: **KHÔNG tồn tại** (đã find toàn bộ Downloads) → báo cáo này dùng source + test + benchmark artifacts làm bằng chứng, không có blueprint đối chiếu.

## SKILL BINDING (SHA256)

| File | SHA256 |
|---|---|
| `.agents/skills/scp-dna/SKILL.md` | `4aada0be4873598dc50c3a7f38d90151429bb5263c511a838ed1cdcb4d594d10` |
| `.agents/skills/scp-reality-verifier/SKILL.md` | `a9d65ce53b18f8310ceeb302b18b341a0e1b8b19cddc7f46d4fa6ee99432269e` |

Áp dụng: scp-dna core loop (evidence-first, missing-piece, reality-test) + scp-reality-verifier (phân biệt Static-A / Integration-B / End-to-end-C; static PASS không nâng cấp thành runtime proof).

## BẢNG AUDIT 11 MẢNH

| # | Mảnh | 1. EXISTS | 2. WIRED_IN_RUNTIME | 3. EVIDENCE + FALSIFIABLE? | 4. GAP | 5. DECISION |
|---|---|---|---|---|---|---|
| 1 | **Knowledge domain store** `scp/knowledge/domain_store.py` (507 LOC) | CÓ, thật: SHA-256 per-record L172/198, dedup L171-180, TTL L124-128, tamper-detect API L383-492, không stub | **KHÔNG**. `RealityJudge.domain_knowledge_store` trả `None` (`scp/runtime/judge.py:111`) → phase1 KB retrieval (`judge_parts/phases/phase1_kb_retrieval.py:15-17`) và phase9 store (`phase9_claim_extraction.py:154-156`) là nhánh chết; `/v100/knowledge/*` luôn 503 (`api/routes/admin_v100.py:94,104`). Không nơi nào `DomainKnowledgeStore(...)` được khởi tạo | `tests/test_subsystem_knowledge.py:12` chỉ assert import được — **KHÔNG falsifiable** với bug logic | Judge trả None → toàn bộ KB nội bộ không đọc/ghi trong prod. TODO tự nhận chưa wire lifespan tại `domain_store.py:370-382` | **NỐI** (wire store vào judge + lifespan; nhỏ) |
| 2 | **Curation 5-nguồn** `scp/core/knowledge_curation.py` (256) | CÓ, thật: arXiv/HN/SO scrapers L49-137, GitHub/Wiki qua learner L206-207, 4-signal `reliability_score` L144-189, quarantine hard-gate L179-180 | **3/5 KHÔNG**. `curate()` có 0 caller trong scp/tools/tests. Chỉ 2 nguồn (GitHub+Wikipedia) chạy thật qua route admin `/v104/learn/top-systems` (`api/routes/v104_routes.py:438-448`) + autofix enrichment (`autofix/llm_fix_parts/_top_systems_references.py:24`) | Static grep test (`T03 test_flow_14:134-145`) — **KHÔNG falsifiable** cho pipeline; không benchmark chất lượng curation | Pipeline 5-nguồn chỉ tồn tại trên giấy; 2 nguồn còn lại là admin-trigger manual, không scheduler | **NỐI** curate() vào scheduler/route, hoặc hạ claim còn 2-nguồn |
| 3 | **Epistemic ladder L0→L4 (GOLD≠truth)** `scp/knowledge/ontology.py` | CÓ, thật: RAW→CURATED→CORROBORATED→VERIFIED→GOLD L45-65, chặn RAW→GOLD L104-112, GOLD bắt buộc evidence_refs L153-154 (chuỗi đúng "GOLD != eternal truth") | **KHÔNG**. `KnowledgeObject` chỉ được dùng bởi `promotion_contract.py:6`; `PromotionAuthority` chỉ trong tests; 0 module prod tạo/đẩy KnowledgeObject | `T02/test_knowledge_ontology.py:47-65` — **FALSIFIABLE** (RAW→GOLD raise, GOLD thiếu evidence raise, bảng transition đóng) | Ladder là schema authority, không có producer runtime; `CognitiveOrchestrator` (nơi đáng nối) cũng 0 caller | **NỐI** vào ingestion + PromotionAuthority |
| 4 | **Contradiction engine** `scp/knowledge/contradiction_authority.py` (146) | CÓ phầnPersist thật (sqlite L59-75, register L120-146), NHƯNG heuristic assess yếu: so **độ dài chuỗi** timestamp `abs(len(time_a)-len(time_b))==0` (L92); tự nhận chưa trigger UNDER_REVIEW cho HIGH (L143-144) | **KHÔNG**. Chỉ dùng bởi `CognitiveOrchestrator` (L5,23) mà orchestrator không có caller ngoài tests. `KnowledgeControlDB` chỉ tests khởi tạo | `T02/test_contradiction_authority.py` assert 3 relation — **falsifiable một phần**, không kill bug L92 | Không chạy runtime + temporal-check sai bản chất | **REBUILD** phần assess + wire trigger |
| 5 | **Quarantine / trust model** `trust_hierarchy.py` + `inspect_untrusted` | CÓ, thật: TrustTier 0-4 + UNIFIED_SOURCE_REGISTRY L151-191 (fix tier-5 L187-190); quarantine gate trong curation L179-180 | **MỘT NHÁNH CÓ**: semantic firewall chạy thật trong `/ask` — `api_server_parts/_ask_impl.py:358-367` quarantine injection trong evidence trước judge. Ngược lại tier/TTL chỉ tác động qua domain_store (đang chết) và `SourceWatchlist.ingestion_decision` (`core/conflict_resolver.py:104`) chỉ dùng cho crypto conflict | Semantic-firewall path là runtime-B; tier tests unit. Chưa có test E2E poison-KB | Tier không có tác động thực vì KB chết; reputation/watchlist không gọi từ ingestion thật | **GIỮ-NHƯ-LÀ** firewall; nối tier khi mảnh #1 sống |
| 6 | **Lineage + sha256 provenance** `scp/epistemic/lineage.py` (289) + `evidence_store.py` (320) | CÓ, thật: DB-enforced `UNKNOWN_INDEPENDENCE` default (migration 0003 L25-43, insert bỏ cột status L74-82); sha256 content-addressed blobs (evidence_store L110-113, L313) | **EvidenceStore CÓ**: `api/routes/v106_routes.py:46`, router đăng ký `api_server.py:400`; GovernedEvidenceWriter dùng trong `llm_gateway/free_catalog.py:21-22`; `governance/retention.py:16`. **LineageStore KHÔNG**: 0 caller runtime, chỉ tests/T06 + tool verify | `T06/test_source_identity_lineage.py:27-84` — **FALSIFIABLE** (DB default unknown, không thể tự phong INDEPENDENT) | LineageStore tách rời EvidenceStore → `independent_lineages` của ontology không có nguồn dữ liệu runtime | **NỐI** LineageStore vào evidence/ingestion |
| 7 | **Continual learning** `scp/learning/continual.py` (36) | YẾU + **BỊ HỎNG**: gọi `self._engine.record_insight(...)` (L28) nhưng `def record_insight` **không tồn tại** trong toàn repo → AttributeError nếu chạy | **KHÔNG** — 0 caller | Không có | Dead code + broken API | **BỎ** (chức năng thật do FastLearningEngine đảm nhiệm, xem ghi chú dưới) |
| 8 | **Knowledge Warehouse (FAISS)** `scp/knowledge/warehouse.py` (34) | **STUB**: "Placeholder for actual embedding logic" L26, "Placeholder for similarity search" L32; không FAISS | **KHÔNG** — singleton tự tạo, 0 caller | Không có | Placeholder thuần | **BỎ** (nếu cần semantic cache, `capabilities/vector_db.py` là bản thật hơn) |
| 9 | **RAG / knowledge ingestion (RAG-1000)** `scp/rag/canonical_retriever.py` (76, TF-IDF thật) + `capabilities/rag.py` + `capabilities/vector_db.py` | Retriever thật nhưng **corpus 0**: `data/rag_corpus/` trống trong checkout (paths L33). `/v105/rag/query` import `HybridRetriever` **KHÔNG TỒN TẠI** ở bất kỳ đâu (`api/routes/v105_routes.py:719,725`) → 500 khi gọi. `RAGEngine` (capabilities/rag.py) 0 caller. Patch auto-retrieval cho /ask chỉ là tool chưa áp (`tools/patch_auto_canonical_retrieval_v1.py`; không có `get_canonical_retriever` trong `api_server.py`/`_ask_impl.py`) | **KHÔNG** (corpus nội bộ). Live retrieval 4-API (Wikipedia/Wikidata/arXiv/DDG) CÓ wired qua DomainSLM fallback: `runtime/slms_parts/_domainslm.py:288` `fetch_live` | **CÓ, FALSIFIABLE**: `D_evidence_recall` benchmark live-server seeded — `reports/benchmark-2026-09-12/bench_after_fix_seed42.json` = **0.3846** (13 gold), `bench_final_seed99.json` = **0.0** (8 gold), method "retrieved_relevant/gold_evidence". RAG-1000 gate: `archive/.../phase10_release_evidence_gate_final.json:35` = "BLOCKED"; kết quả cũ đã bị dọn theo `benchmark/CLEANUP_MANIFEST_20260817.json:5-9` | Corpus 0 + endpoint gãy import + test `T03/test_flow_14:134-145` chỉ grep text "HybridRetriever" trong file route nên **PASS dù endpoint 500** — không kill được product bug | **REBUILD** (slice 2 dưới đây) |
| 10 | **Question/hypothesis memory** `open_question_authority.py` (120) + `hypothesis_authority.py` (62) + `doubt_engine.py` (104) | CÓ, thật (dataclass + sqlite store) | **KHÔNG**. `EpistemicBoundary` (consumer duy nhất của OpenQuestion) 0 caller; `DoubtAuthority` 0 caller; chỉ `CognitiveOrchestrator` (tự cũng dead) | Unit tests contract-level, falsifiable cục bộ | Cả cụm meta-cognitive chỉ tồn tại dưới dạng tests-only | **GIỮ code, NỐI sau** khi mảnh #3 sống |
| 11 | *(tham chiếu)* **Online learning thật** `FastLearningEngine` | Thật: wiki cross-check trước khi store (`fast_learning_engine_parts/fastlearningengine.py:460,571`), `_store_kb` L305 | **CÓ** — thread khởi động trong `api_server_parts/helpers.py:367` (post G3-MERGE, idempotent), ghi `data/v13.db` | Chạy thật trong prod judge init | KB v13.db này KHÔNG được đọc lại bởi judge (phase1 đọc domain_store=None) → học xong không dùng | **NỐI** output của nó vào retrieval |

## VERDICT TỔNG

**Kiến trúc bằng chứng nhóm KNOWLEDGE/LEARNING: đỏ ở tầng WIRED, không đỏ ở tầng EXISTS.** Đa số module ghi đĩa (domain store, ladder, contradiction, lineage, question/hypothesis) đều là code thật, test thật (nhiều test falsifiable tốt: ontology, lineage), nhưng **không có mảnh nào trong số đó chạy trong prod path** — chuỗi nối bị đứt tại đúng một điểm: `RealityJudge.domain_knowledge_store` → `None` (judge.py:111). Hệ quả dây chuyền: crawler (`judge.py:319` trả `{}`), KB short-circuit, claim extraction, tier/TTL, promotion ladder đều thành dead code. Trong khi đó duy nhất 2 dòng chảy học hỏi sống: (a) FastLearningEngine thread ghi v13.db (không ai đọc lại), (b) semantic firewall + live 4-API fetch trong /ask.

Hai bug "test-pass nhưng product-gãy" tiêu biểu (đúng DNA: static PASS ≠ runtime proof):
1. `v105_routes.py:719` — import `HybridRetriever` không tồn tại → `/v105/rag/query` 500; test `test_flow_14` chỉ grep text nên vẫn PASS.
2. `continual.py:28` — gọi method không tồn tại (`record_insight`); không ai gọi nên chưa nổ.

Ghi nhận bằng chứng âm (không tìm thấy): blueprint 2026-09-02 không có trên máy; `data/rag_corpus/` trống; `data/benchmark_batches/` trống; release gate `rag_1000: BLOCKED`.

**Scope limit:** audit tĩnh + đọc artifacts benchmark có sẵn; KHÔNG chạy server, KHÔNG chạy lại benchmark, KHÔNG test E2E. Verdict chỉ đạt cấp Static/Integration theo thang reality-verifier; mọi số recall/0.38 là artifact lịch sử, không phải runtime proof hôm nay.

## TOP-2 DỌC-SLICE NHỎ NHẤT CÓ BENCHMARK

### Slice 1 — "Sống lại KB nội bộ" (nối domain_store vào judge)
- **Ô phải nối:** `RealityJudge.domain_knowledge_store` (judge.py:111) → instance thật `DomainKnowledgeStore(data/knowledge)` khởi tạo trong lifespan (theo TODO domain_store.py:370) + `phase9_claim_extraction.py:156` store lại có tác dụng.
- **Size ước lượng:** ~40-80 dòng (1 property + 1 lifespan block + integrity job), không đụng pipeline chính.
- **Benchmark đo:** `/v100/knowledge/stats` chuyển 503→200 có số liệu; live bench hiện có (harness `reports/benchmark-2026-09-12/*`) thêm metric `kb_hits`/`kb_short_circuit` trên bộ seed42/99; kill condition: test integration assert `judge.domain_knowledge_store.search("câu vừa store")` trả record — hiện tại test này phải FAIL trước khi patch, PASS sau.

### Slice 2 — "RAG retrieval thật trên /ask" (sửa import + corpus tối thiểu)
- **Ô phải nối:** (1) sửa `v105_routes.py:719` `HybridRetriever` → `CanonicalRetriever` (đổi signature `search(query, top_k)` → `retrieve(q,k)`); (2) áp patch auto-retrieval `tools/patch_auto_canonical_retrieval_v1.py` vào `_ask_impl` để contexts lấy từ corpus khi `req.rag_enabled`; (3) dựng lại `data/rag_corpus/canonical-v3/verified_seed_corpus.jsonl` tối thiểu (bộ verified seed cũ) — hiện 0 docs.
- **Size ước lượng:** ~60-120 dòng + 1 file corpus seed; corpus rebuild lớn nhất là phần dữ liệu, không phải code.
- **Benchmark đo:** `D_evidence_recall` trên cùng harness live seeded (baseline hiện tại: 0.3846 seed42 / 0.0 seed99 — bench_after_fix_seed42.json, bench_final_seed99.json) + `tools/eval_retrieval_standard.py` (recall@1/5/10, MRR, nDCG với gold annotations; verdict tự khai "BASELINE_ONLY_NOT_FACTUAL_PROOF" — giữ nguyên disclaimer). Kill: import sai → endpoint 500 bắt được bằng TestClient thay vì grep text.

## OPEN QUESTIONS (chưa giải được trong phạm vi)
1. Corpus canonical-v2/v3 gốc nằm ở đâu (bị dọn theo manifest nào) — cần provenance trước khi dựng lại slice 2.
2. `data/v13.db` đang chứa bao nhiêu fact FastLearningEngine đã học và có nên migrate sang domain_store khi nối (slice 1).
3. Việc `test_flow_14` dạng grep-text (và các test `pass` placeholder ở L319-327 cùng file) có thuộc diện "harness phải siết thêm" theo hợp đồng test-skill — để Agent harness quyết.
