I001 [*] Import block is un-sorted or un-formatted
  --> scp\api_server.py:22:1
   |
20 |     GET  /                             Ă„â€Ă‚Â¢Ä‚Â¢Ă¢â‚¬ÂĂ‚Â¬Ä‚Â¢Ă¢â€Â¬Ă‚Â Root info
21 |   """
22 | / from __future__ import annotations
23 | |
24 | | from scp.security.env_loader import load_selected_env
   | |_____________________________________________________^
25 |   load_selected_env()
   |
help: Organize imports
   |
24 | from scp.security.env_loader import load_selected_env
25 +
26 | load_selected_env()
   |

I001 [*] Import block is un-sorted or un-formatted
  --> scp\api_server.py:38:5
   |
36 |   def _scp_service_identity() -> dict:
37 |       """Expose bounded runtime identity for local service/port verification."""
38 | /     import hashlib as _hashlib
39 | |     import subprocess as _subprocess
40 | |     from pathlib import Path as _Path
41 | |     import sys as _sys
   | |______________________^
42 |       try:
43 |           _port = int(os.environ.get("SCP_PORT", "8000"))
   |
help: Organize imports
   |
39 |     import subprocess as _subprocess
40 +     import sys as _sys
41 |     from pathlib import Path as _Path
   -     import sys as _sys
42 |     try:
   |

I001 [*] Import block is un-sorted or un-formatted
  --> scp\api_server.py:88:1
   |
86 |   logger = logging.getLogger("scp.api")
87 |
88 | / from scp.web_control.internet_search import InternetSearch
89 | |
90 | | from scp.api_server_parts.helpers import (
91 | |     AskRequest,
92 | |     AskResponse,
93 | |     _extract_v98_context,
94 | |     _safe_fetch_url,
95 | |     get_judge,
96 | | )
97 | | from scp.core.request_run_ledger import RequestRunLedger, stage_request, traced_request
   | |_______________________________________________________________________________________^
98 |
99 |   _REQUEST_RUN_LEDGER = RequestRunLedger()
   |
help: Organize imports
   |
87 |
   - from scp.web_control.internet_search import InternetSearch
   -
88 | from scp.api_server_parts.helpers import (
--------------------------------------------------------------------------------
95 | from scp.core.request_run_ledger import RequestRunLedger, stage_request, traced_request
96 + from scp.web_control.internet_search import InternetSearch
97 |
   |

I001 [*] Import block is un-sorted or un-formatted
   --> scp\api_server.py:531:9
    |
529 |       app.state.attack_monitor_started = False
530 |       try:
531 | /         import threading as _threading
532 | |         import time as _time
533 | |         from scp.autofix.runner import run_deep_audit
534 | |         from scp.core.subsystem_telemetry import SubsystemTelemetry, heartbeat_sleep
    | |____________________________________________________________________________________^
535 |
536 |           _audit_telemetry = SubsystemTelemetry("deep_audit", os.environ.get("SCP_DATA_DIR", "data"))
    |
help: Organize imports
    |
532 |         import time as _time
533 +
534 |         from scp.autofix.runner import run_deep_audit
    |

F401 [*] `os` imported but unused
    --> scp\api_server.py:1616:18
     |
1614 |     # DNA #7 (Autofix safe) + #26 (Reality > Model): server alive = 200, not
1615 |     #   "wait for all subsystems to be perfect".
1616 |     import os as _os
     |                  ^^^
1617 |     return {
1618 |         "status": "ok",
     |
help: Remove unused import: `os`
     |
1615 |     #   "wait for all subsystems to be perfect".
     -     import os as _os
1616 |     return {
     |

F401 [*] `pathlib.Path` imported but unused
 --> scp\ask_kernel_adapter.py:8:21
  |
6 | import threading
7 | import uuid
8 | from pathlib import Path
  |                     ^^^^
9 | from typing import Any, Awaitable, Callable
  |
help: Remove unused import: `pathlib.Path`
  |
7 | import uuid
  - from pathlib import Path
8 | from typing import Any, Awaitable, Callable
  |

UP035 [*] Import from `collections.abc` instead: `Awaitable`, `Callable`
  --> scp\ask_kernel_adapter.py:9:1
   |
 7 | import uuid
 8 | from pathlib import Path
 9 | from typing import Any, Awaitable, Callable
   | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
10 |
11 | try:
   |
help: Import from `collections.abc`
   |
8  | from pathlib import Path
   - from typing import Any, Awaitable, Callable
9  + from typing import Any
10 + from collections.abc import Awaitable, Callable
11 |
   |

I001 [*] Import block is un-sorted or un-formatted
  --> scp\task_kernel.py:1:1
   |
 1 | / from __future__ import annotations
 2 | |
 3 | | import hashlib
 4 | | import json
 5 | | import secrets
 6 | | import sqlite3
 7 | | import time
 8 | | from dataclasses import asdict, dataclass
 9 | | from datetime import datetime, timezone
10 | | from pathlib import Path
11 | | from typing import Any
   | |______________________^
help: Organize imports
   |
12 |
   -
13 | STATES = {
   |

F601 Dictionary key literal `"WAITING_APPROVAL"` repeated
  --> scp\task_kernel.py:36:5
   |
34 |     "RECONCILING": {"RECOVERING", "CHECKPOINTED", "QUEUED", "HUMAN_REVIEW", "FAILED", "CANCELLED"},
35 |     "RETRY_SCHEDULED": {"QUEUED", "FAILED", "CANCELLED"},
36 |     "WAITING_APPROVAL": {"READY", "CANCELLED"},
   |     ^^^^^^^^^^^^^^^^^^
37 |     "COMPLETED": set(), "FAILED": set(), "CANCELLED": set(),
38 | }
   |
help: Remove repeated key literal `"WAITING_APPROVAL"`

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:246:28
    |
244 |         if risk_tier not in {"R0", "R1", "R2", "R3"}:
245 |             raise KernelError("invalid risk tier")
246 |         created = now_iso(); input_hash = input_hash or stable_hash({"goal": goal})
    |                            ^
247 |         self._begin()
248 |         try:
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:273:39
    |
271 |                     self.conn.execute("COMMIT")
272 |                     return self.get_task(task_id)
273 |             task = self._task(task_id); old = task["state"]
    |                                       ^
274 |             if to_state not in ALLOWED_TRANSITIONS.get(old, set()):
275 |                 raise InvalidTransition(f"{old}->{to_state}")
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:297:48
    |
295 |         self._begin()
296 |         try:
297 |             control = self._assert_not_killed(); task = self._task(task_id)
    |                                                ^
298 |             created_at = datetime.fromisoformat(task["created_at"]).timestamp()
299 |             if time.time() >= created_at + (int(task["deadline_ms"]) / 1000.0):
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:305:33
    |
303 | â€¦     attempt_id = attempt_id or "attempt_" + secrets.token_hex(8)
304 | â€¦     old = self.conn.execute("SELECT COALESCE(MAX(fencing_token),0) AS n FROM leases WHERE task_id=?", (task_id,)).fetchone()["n"]
305 | â€¦     token = int(old) + 1; now = time.time(); lease_id = "lease_" + secrets.token_hex(12)
    |                           ^
306 | â€¦     self.conn.execute(
307 | â€¦         "INSERT INTO leases(lease_id,task_id,attempt_id,worker_id,issued_at,expires_at,heartbeat_at,fencing_token,global_kill_epochâ€¦
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:305:52
    |
303 | â€¦     attempt_id = attempt_id or "attempt_" + secrets.token_hex(8)
304 | â€¦     old = self.conn.execute("SELECT COALESCE(MAX(fencing_token),0) AS n FROM leases WHERE task_id=?", (task_id,)).fetchone()["n"]
305 | â€¦     token = int(old) + 1; now = time.time(); lease_id = "lease_" + secrets.token_hex(12)
    |                                              ^
306 | â€¦     self.conn.execute(
307 | â€¦         "INSERT INTO leases(lease_id,task_id,attempt_id,worker_id,issued_at,expires_at,heartbeat_at,fencing_token,global_kill_epochâ€¦
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:325:38
    |
324 |     def _assert_lease(self, lease_id: str, task_id: str) -> sqlite3.Row:
325 |         lease = self._lease(lease_id); control = self._control(); now = time.time()
    |                                      ^
326 |         if lease["task_id"] != task_id or lease["released"] or lease["expires_at"] <= now or lease["global_kill_epoch"] != control["gâ€¦
327 |             raise StaleLease(lease_id)
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:325:65
    |
324 |     def _assert_lease(self, lease_id: str, task_id: str) -> sqlite3.Row:
325 |         lease = self._lease(lease_id); control = self._control(); now = time.time()
    |                                                                 ^
326 |         if lease["task_id"] != task_id or lease["released"] or lease["expires_at"] <= now or lease["global_kill_epoch"] != control["gâ€¦
327 |             raise StaleLease(lease_id)
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:338:50
    |
336 |         self._begin()
337 |         try:
338 |             self._assert_lease(lease_id, task_id); task = self._task(task_id)
    |                                                  ^
339 |             if task["state"] != "LEASED": raise InvalidTransition(f"{task['state']}->RUNNING")
340 |             self.conn.execute("UPDATE tasks SET state='RUNNING',version=version+1,updated_at=? WHERE task_id=?", (now_iso(), task_id))
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:344:42
    |
342 |             self.conn.execute("COMMIT")
343 |         except Exception:
344 |             self.conn.execute("ROLLBACK"); raise
    |                                          ^
345 |         return self.get_task(task_id)
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:350:58
    |
348 |         self._begin()
349 |         try:
350 |             lease = self._assert_lease(lease_id, task_id); now = time.time(); expires = now + extend_seconds
    |                                                          ^
351 |             self.conn.execute("UPDATE leases SET heartbeat_at=?,expires_at=? WHERE lease_id=?", (now, expires, lease_id))
352 |             self.conn.execute("COMMIT")
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:350:77
    |
348 |         self._begin()
349 |         try:
350 |             lease = self._assert_lease(lease_id, task_id); now = time.time(); expires = now + extend_seconds
    |                                                                             ^
351 |             self.conn.execute("UPDATE leases SET heartbeat_at=?,expires_at=? WHERE lease_id=?", (now, expires, lease_id))
352 |             self.conn.execute("COMMIT")
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:355:42
    |
353 |             return Lease(lease["lease_id"], lease["task_id"], lease["attempt_id"], lease["worker_id"], expires, lease["fencing_token"â€¦
354 |         except Exception:
355 |             self.conn.execute("ROLLBACK"); raise
    |                                          ^
356 |
357 |     def expire_leases(self, now: float | None = None) -> list[str]:
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:358:33
    |
357 |     def expire_leases(self, now: float | None = None) -> list[str]:
358 |         now = now or time.time(); expired=[]; self._begin()
    |                                 ^
359 |         try:
360 |             for lease in self.conn.execute("SELECT * FROM leases WHERE released=0 AND expires_at<=?", (now,)).fetchall():
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:358:45
    |
357 |     def expire_leases(self, now: float | None = None) -> list[str]:
358 |         now = now or time.time(); expired=[]; self._begin()
    |                                             ^
359 |         try:
360 |             for lease in self.conn.execute("SELECT * FROM leases WHERE released=0 AND expires_at<=?", (now,)).fetchall():
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:364:38
    |
362 | â€¦     task = self._task(lease["task_id"])
363 | â€¦     if task["state"] in {"LEASED", "RUNNING", "WAITING_TOOL"}:
364 | â€¦         old=task["state"]; self.conn.execute("UPDATE tasks SET state='RECOVERING',version=version+1,updated_at=? WHERE task_id=?", â€¦
    |                            ^
365 | â€¦         self._append_event(task["task_id"], "LEASE_EXPIRED", old, "RECOVERING", "kernel", "heartbeat_expired", {"lease_id": lease["â€¦
366 | â€¦     self.conn.execute("UPDATE leases SET released=1 WHERE lease_id=?", (lease["lease_id"],))
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:370:42
    |
368 |             return expired
369 |         except Exception:
370 |             self.conn.execute("ROLLBACK"); raise
    |                                          ^
371 |
372 |     def release(self, task_id: str, lease_id: str) -> None:
    |

F841 Local variable `lease` is assigned to but never used
   --> scp\task_kernel.py:375:13
    |
373 | â€¦     self._begin()
374 | â€¦     try:
375 | â€¦         lease=self._assert_lease(lease_id,task_id);self.conn.execute("UPDATE leases SET released=1 WHERE lease_id=?",(lease_id,));sâ€¦
    |           ^^^^^
376 | â€¦     except Exception:
377 | â€¦         self.conn.execute("ROLLBACK");raise
    |
help: Remove assignment to unused variable `lease`

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:375:55
    |
373 | â€¦     self._begin()
374 | â€¦     try:
375 | â€¦         lease=self._assert_lease(lease_id,task_id);self.conn.execute("UPDATE leases SET released=1 WHERE lease_id=?",(lease_id,));sâ€¦
    |                                                     ^
376 | â€¦     except Exception:
377 | â€¦         self.conn.execute("ROLLBACK");raise
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:375:134
    |
373 | â€¦
374 | â€¦
375 | â€¦cute("UPDATE leases SET released=1 WHERE lease_id=?",(lease_id,));self._append_event(task_id,"LEASE_RELEASED",None,None,"kernel","woâ€¦
    |                                                                   ^
376 | â€¦
377 | â€¦
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:375:237
    |
373 | â€¦
374 | â€¦
375 | â€¦EASED",None,None,"kernel","worker_release",{"lease_id":lease_id});self.conn.execute("COMMIT")
    |                                                                   ^
376 | â€¦
377 | â€¦
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:377:42
    |
375 |             lease=self._assert_lease(lease_id,task_id);self.conn.execute("UPDATE leases SET released=1 WHERE lease_id=?",(lease_id,))â€¦
376 |         except Exception:
377 |             self.conn.execute("ROLLBACK");raise
    |                                          ^
378 |
379 |     def checkpoint(self, task_id: str, lease_id: str, step_id: str, state: str, planned_action: Any,
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:386:55
    |
384 | â€¦     self._begin()
385 | â€¦     try:
386 | â€¦         lease=self._assert_lease(lease_id,task_id); payload={"task_id":task_id,"attempt_id":lease["attempt_id"],"step_id":step_id,"â€¦
    |                                                     ^
387 | â€¦         self.conn.execute("INSERT INTO checkpoints(checkpoint_id,task_id,attempt_id,step_id,state,planned_action_hash,capability_epâ€¦
388 | â€¦         self._append_event(task_id,"CHECKPOINT_WRITTEN",None,state,"kernel","checkpoint_written",{"checkpoint_id":cp_id,"payload_haâ€¦
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:386:399
    |
384 | â€¦
385 | â€¦
386 | â€¦ef,"tool_result":tool_result,"verifier_verdict":verifier_verdict};cp_id="cp_"+secrets.token_hex(10);payload_hash=stable_hash(payload)
    |                                                                   ^
387 | â€¦id,state,stable_hash(planned_action),capability_epoch,idempotency_key,pre_observation_ref,post_observation_ref,json.dumps(tool_resulâ€¦
388 | â€¦
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:386:433
    |
384 | â€¦
385 | â€¦
386 | â€¦fier_verdict":verifier_verdict};cp_id="cp_"+secrets.token_hex(10);payload_hash=stable_hash(payload)
    |                                                                   ^
387 | â€¦n),capability_epoch,idempotency_key,pre_observation_ref,post_observation_ref,json.dumps(tool_result,ensure_ascii=False,sort_keys=Truâ€¦
388 | â€¦
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:389:40
    |
387 | â€¦         self.conn.execute("INSERT INTO checkpoints(checkpoint_id,task_id,attempt_id,step_id,state,planned_action_hash,capability_epâ€¦
388 | â€¦         self._append_event(task_id,"CHECKPOINT_WRITTEN",None,state,"kernel","checkpoint_written",{"checkpoint_id":cp_id,"payload_haâ€¦
389 | â€¦         self.conn.execute("COMMIT");return cp_id
    |                                      ^
390 | â€¦     except Exception:
391 | â€¦         self.conn.execute("ROLLBACK");raise
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:391:42
    |
389 |             self.conn.execute("COMMIT");return cp_id
390 |         except Exception:
391 |             self.conn.execute("ROLLBACK");raise
    |                                          ^
392 |
393 |     def validate_checkpoint(self, checkpoint_id: str, planned_action: Any) -> dict[str, Any]:
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:404:48
    |
402 | â€¦     try:
403 | â€¦         row=self.conn.execute("SELECT * FROM idempotency WHERE logical_key=?",(logical_key,)).fetchone()
404 | â€¦         if row: self.conn.execute("COMMIT");return logical_key,False
    |                                              ^
405 | â€¦         self.conn.execute("INSERT INTO idempotency(logical_key,task_id,step_id,action_type,resource_identity,status,created_at) VALâ€¦
406 | â€¦     except Exception:
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:405:237
    |
403 | â€¦
404 | â€¦
405 | â€¦sk_id,step_id,action_type,resource_identity,"CLAIMED",now_iso()));self.conn.execute("COMMIT");return logical_key,True
    |                                                                   ^
406 | â€¦
407 | â€¦
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:405:265
    |
403 | â€¦
404 | â€¦
405 | â€¦source_identity,"CLAIMED",now_iso()));self.conn.execute("COMMIT");return logical_key,True
    |                                                                   ^
406 | â€¦
407 | â€¦
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:407:42
    |
405 |             self.conn.execute("INSERT INTO idempotency(logical_key,task_id,step_id,action_type,resource_identity,status,created_at) Vâ€¦
406 |         except Exception:
407 |             self.conn.execute("ROLLBACK");raise
    |                                          ^
408 |
409 |     def idempotency_complete(self, logical_key: str, result_ref: str) -> None:
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:423:49
    |
421 | â€¦     self._begin()
422 | â€¦     try:
423 | â€¦         self._assert_lease(lease_id,task_id);task=self._task(task_id)
    |                                               ^
424 | â€¦         if task["state"] not in {"VERIFYING","RUNNING"}: raise InvalidTransition(f"{task['state']}->COMPLETED")
425 | â€¦         old=task["state"];self.conn.execute("UPDATE tasks SET state='COMPLETED',version=version+1,updated_at=? WHERE task_id=?",(noâ€¦
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:425:30
    |
423 | â€¦         self._assert_lease(lease_id,task_id);task=self._task(task_id)
424 | â€¦         if task["state"] not in {"VERIFYING","RUNNING"}: raise InvalidTransition(f"{task['state']}->COMPLETED")
425 | â€¦         old=task["state"];self.conn.execute("UPDATE tasks SET state='COMPLETED',version=version+1,updated_at=? WHERE task_id=?",(noâ€¦
    |                            ^
426 | â€¦     except Exception:
427 | â€¦         self.conn.execute("ROLLBACK");raise
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:425:153
    |
423 | â€¦
424 | â€¦{task['state']}->COMPLETED")
425 | â€¦sion=version+1,updated_at=? WHERE task_id=?",(now_iso(),task_id));self._append_event(task_id,"TASK_COMPLETED",old,"COMPLETED","verifâ€¦
    |                                                                   ^
426 | â€¦
427 | â€¦
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:425:336
    |
423 | â€¦
424 | â€¦
425 | â€¦nce_ref,"verifier_verdict":verifier_verdict,"lease_id":lease_id});self.conn.execute("UPDATE leases SET released=1 WHERE lease_id=?",â€¦
    |                                                                   ^
426 | â€¦
427 | â€¦
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:425:415
    |
423 | â€¦
424 | â€¦
425 | â€¦cute("UPDATE leases SET released=1 WHERE lease_id=?",(lease_id,));self.conn.execute("COMMIT");return self.get_task(task_id)
    |                                                                   ^
426 | â€¦
427 | â€¦
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:425:443
    |
423 | â€¦
424 | â€¦
425 | â€¦ased=1 WHERE lease_id=?",(lease_id,));self.conn.execute("COMMIT");return self.get_task(task_id)
    |                                                                   ^
426 | â€¦
427 | â€¦
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:427:42
    |
425 |             old=task["state"];self.conn.execute("UPDATE tasks SET state='COMPLETED',version=version+1,updated_at=? WHERE task_id=?",(â€¦
426 |         except Exception:
427 |             self.conn.execute("ROLLBACK");raise
    |                                          ^
428 |
429 |     def set_global_kill(self, active: bool, actor: str = "operator") -> int:
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:432:30
    |
430 | â€¦     self._begin()
431 | â€¦     try:
432 | â€¦         c=self._control();epoch=int(c["global_kill_epoch"])+(1 if active else 0);self.conn.execute("UPDATE control SET global_kill=â€¦
    |                            ^
433 | â€¦     except Exception:
434 | â€¦         self.conn.execute("ROLLBACK");raise
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:432:85
    |
430 | â€¦     self._begin()
431 | â€¦     try:
432 | â€¦         c=self._control();epoch=int(c["global_kill_epoch"])+(1 if active else 0);self.conn.execute("UPDATE control SET global_kill=â€¦
    |                                                                                   ^
433 | â€¦     except Exception:
434 | â€¦         self.conn.execute("ROLLBACK");raise
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:432:197
    |
430 | â€¦
431 | â€¦
432 | â€¦ill=?,global_kill_epoch=? WHERE id=1",(1 if active else 0,epoch));self._append_event("__global__","GLOBAL_KILL_ON" if active else "Gâ€¦
    |                                                                   ^
433 | â€¦
434 | â€¦
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:432:330
    |
430 | â€¦
431 | â€¦
432 | â€¦OBAL_KILL_OFF",None,None,actor,"operator_toggle",{"epoch":epoch});self.conn.execute("COMMIT");return epoch
    |                                                                   ^
433 | â€¦
434 | â€¦
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:432:358
    |
430 | â€¦
431 | â€¦
432 | â€¦or,"operator_toggle",{"epoch":epoch});self.conn.execute("COMMIT");return epoch
    |                                                                   ^
433 | â€¦
434 | â€¦
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:434:42
    |
432 |             c=self._control();epoch=int(c["global_kill_epoch"])+(1 if active else 0);self.conn.execute("UPDATE control SET global_kilâ€¦
433 |         except Exception:
434 |             self.conn.execute("ROLLBACK");raise
    |                                          ^
435 |
436 |     def set_task_kill(self, task_id: str, actor: str = "operator") -> dict[str, Any]:
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:442:135
    |
440 | â€¦
441 | â€¦
442 | â€¦sion=version+1,updated_at=? WHERE task_id=?",(now_iso(),task_id));self.conn.execute("UPDATE leases SET released=1 WHERE task_id=?",(â€¦
    |                                                                   ^
443 | â€¦
444 | â€¦
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:442:212
    |
440 | â€¦
441 | â€¦
442 | â€¦xecute("UPDATE leases SET released=1 WHERE task_id=?",(task_id,));self._append_event(task_id,"TASK_KILLED",task["state"],"CANCELLED"â€¦
    |                                                                   ^
443 | â€¦
444 | â€¦
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:442:301
    |
440 | â€¦
441 | â€¦
442 | â€¦_id,"TASK_KILLED",task["state"],"CANCELLED",actor,"task_kill",{});self.conn.execute("COMMIT");return self.get_task(task_id)
    |                                                                   ^
443 | â€¦
444 | â€¦
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:442:329
    |
440 | â€¦
441 | â€¦
442 | â€¦e"],"CANCELLED",actor,"task_kill",{});self.conn.execute("COMMIT");return self.get_task(task_id)
    |                                                                   ^
443 | â€¦
444 | â€¦
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:444:42
    |
442 |             self.conn.execute("UPDATE tasks SET state='CANCELLED',version=version+1,updated_at=? WHERE task_id=?",(now_iso(),task_id)â€¦
443 |         except Exception:
444 |             self.conn.execute("ROLLBACK");raise
    |                                          ^
445 |
446 |     def get_task(self, task_id: str) -> dict[str, Any]: return dict(self._task(task_id))
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:453:40
    |
451 |         return dict(row)
452 |     def verify_journal(self, task_id: str) -> dict[str, Any]:
453 |         events=self.get_events(task_id);prev=None;errors=[]
    |                                        ^
454 |         for i,e in enumerate(events,1):
455 |             if e["seq"]!=i: errors.append(f"sequence:{e['seq']} expected {i}")
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:453:50
    |
451 |         return dict(row)
452 |     def verify_journal(self, task_id: str) -> dict[str, Any]:
453 |         events=self.get_events(task_id);prev=None;errors=[]
    |                                                  ^
454 |         for i,e in enumerate(events,1):
455 |             if e["seq"]!=i: errors.append(f"sequence:{e['seq']} expected {i}")
    |

E702 Multiple statements on one line (semicolon)
   --> scp\task_kernel.py:468:109
    |
466 |         for e in events[1:]:
467 |             if e["to_state"]: state=e["to_state"]
468 |         self.conn.execute("UPDATE tasks SET state=?,updated_at=? WHERE task_id=?",(state,now_iso(),task_id));return self.get_task(tasâ€¦
    |                                                                                                             ^
469 |
470 |     @staticmethod
    |

I001 [*] Import block is un-sorted or un-formatted
 --> scp\trace_ledger.py:1:1
  |
1 | / from __future__ import annotations
2 | |
3 | | import hashlib
4 | | import json
5 | | import secrets
6 | | from pathlib import Path
7 | | from typing import Any
  | |______________________^
help: Organize imports
  |
8 |
  -
9 | _SECRET_WORDS=("secret", "token", "password", "cookie", "api_key", "authorization", "private_key")
  |

E702 Multiple statements on one line (semicolon)
  --> scp\trace_ledger.py:27:29
   |
25 | class TraceLedger:
26 |     def __init__(self, path: str | Path):
27 |         self.path=Path(path);self.path.parent.mkdir(parents=True,exist_ok=True)
   |                             ^
28 |     def append(self, **fields: Any) -> dict[str, Any]:
29 |         lines=self.path.read_text(encoding="utf-8").splitlines() if self.path.exists() else []
   |

E702 Multiple statements on one line (semicolon)
  --> scp\trace_ledger.py:37:18
   |
35 |     def verify(self) -> dict[str, Any]:
36 |         lines=self.path.read_text(encoding="utf-8").splitlines() if self.path.exists() else []
37 |         errors=[];prev=None
   |                  ^
38 |         for i,line in enumerate(lines,1):
39 |             e=json.loads(line)
   |

I001 [*] Import block is un-sorted or un-formatted
 --> scp\verifier.py:1:1
  |
1 | / from __future__ import annotations
2 | |
3 | | from dataclasses import dataclass, asdict
4 | | from typing import Any
  | |______________________^
help: Organize imports
  |
2 |
  - from dataclasses import dataclass, asdict
3 + from dataclasses import asdict, dataclass
4 | from typing import Any
  |

F401 [*] `asyncio` imported but unused
 --> tests\test_ask_kernel_integration.py:3:8
  |
1 | from __future__ import annotations
2 |
3 | import asyncio
  |        ^^^^^^^
4 | from types import SimpleNamespace
  |
help: Remove unused import: `asyncio`
  |
2 |
  - import asyncio
3 | from types import SimpleNamespace
  |

BLE001 Do not catch blind exception: `Exception`
  --> tests\test_task_kernel_acceptance.py:35:16
   |
33 |             kernel.claim("task", name, ttl_seconds=5)
34 |             value = "OK"
35 |         except Exception as exc:  # only one worker is expected to claim
   |                ^^^^^^^^^
36 |             value = type(exc).__name__
37 |         with lock:
   |

Found 66 errors.
[*] 11 fixable with the `--fix` option (2 hidden fixes can be enabled with the `--unsafe-fixes` option).
