# M03 D2 — Runtime proof at pin 3f29c180502fe71d92454f9ffcf699476a07f4ce

Date: 2026-09-11. Branch: audit/runtime-guard-AUDIT-20260909.

## Commands + results

- `SCP_GIT_SHA=3f29c180502fe71d92454f9ffcf699476a07f4ce docker compose build scp-api`
  -> EXIT=0 (full log: D2-docker-build.txt). Image: scp-api:local =
  sha256:0cb616c8236aaf736afb6313597e74de32ced1dbe5ba9b0a06a4c7955fc98245
- `docker compose up -d --force-recreate scp-api` -> container scp-scp-api-1
  Started. RestartCount=0, StartedAt=2026-09-11T05:19:30.592823025Z.
- GET http://127.0.0.1:8000/health -> HTTP 200
  {"status":"ok", ... "service_identity":{"commit":"3f29c180502fe71d92454f9ffcf699476a07f4ce"}}
  -> service identity commit == sha_pin TRUE.
- GET http://127.0.0.1:8000/readiness -> HTTP 200
  {"status":"ready","service":"scp-api","version":"14.0.0",
   "checks":{"judge":"ok","background_scheduler":"ok"},"reason":null}

## Profile posture on the main deployment (observed, disclosed)

The main deployment runs SCP_API_PROFILE=core (from .env). Route group
"openai_compat" requires profile level >= standard (scp/api/route_profile.py),
so on the MAIN instance:

- POST /v1/chat/completions (no auth) -> HTTP 404 {"detail":"Not Found"}

This is the deployment-profile gap (same class as M02 known_gaps for /chat),
NOT a code defect: the routes are registered when the profile is standard or
full. All M3 HTTP route probes were therefore executed on a TEMPORARY
standard-profile instance from the SAME pinned image (see D3-runtime-setup.txt
+ D3-m3-runtime.json), mirroring the M02 procedure.
