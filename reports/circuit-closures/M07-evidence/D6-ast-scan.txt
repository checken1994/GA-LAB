AST except..:pass scan over 9 scope files (119 except handlers)
silent except:pass blocks: 3
 - scp/api/routes/v105_routes.py:672
 - scp/api/routes/v105_routes.py:678
 - scp/autofix/runner_phases/ast_scan.py:663

Classification (worker MC-GREEN, read at pin 554f43e):
1. v105_routes.py:672 except OSError:pass — best-effort unlink of tmp file during
   ROLLBACK path; the outer `raise` re-raises the original HTTPException — the
   error is NOT swallowed, only tmp cleanup is best-effort (commented in code).
2. v105_routes.py:678 except OSError:pass — same pattern in generic-exception
   path; outer code raises HTTPException(500, ...) from e. Error surfaces.
3. ast_scan.py:663 except Exception:pass — _v3_update_cache fail-open best-effort
   (commented "cache update is best-effort"); scan evidence unaffected; only AST
   diff-cache freshness degrades.
D6 VERDICT: PASS_WITH_LIMITS — 3/3 deliberate best-effort guards; no failure state
silenced (rollback errors re-raise; scan findings independent of cache).
NOTE: v105_routes.py module docstring contains mojibake (broken UTF-8 of the
table glyph rows) — pre-existing cosmetic; does not affect execution.
