# M10 D3 — Runtime setup + probe record (pin 1691f7fcbcd1c21305f79ee06c28d572ab5e0b35)

Date: 2026-09-11. Branch: audit/runtime-guard-AUDIT-20260909. Worker: MC10.
NOTE: matrix nay da chay DUOC LAN 2 (re-run sau anomaly mat file — closure G9);
hai lan deu 12/12 PASS, ket qua deterministic (chi arrival timestamp thay doi
phiên: lan 1 [0.079..0.125]s, lan 2 [0.031..0.093]s).

## Instance (lan 2)

- Temporary container `scp-m10-probe`, image scp-api:local (the SAME pinned
  image verified in D2), port 127.0.0.1:8005, SCP_API_PROFILE=full (override;
  the "stream" route group requires profile "full" per scp/api/route_profile.py),
  torn down after probes.
- Base env: `--env-file .env`, then explicit overrides: SCP_API_PROFILE=full,
  probe-local SCP_AUTH_TOKEN_SECRET (random 32-byte hex per session, NEVER
  printed or persisted), SCP_MODE=isolated, SCP_EGRESS_MODE=deny,
  SCP_PRODUCTION_MODE=0, SCP_FORCE_HTTPS=0, SCP_HOST=0.0.0.0, SCP_PORT=8000,
  SCP_DATA_DIR=/app/data, SCP_PC_WORKING_DIR=/app, image port arg `8000`.
- Startup: /health 200 sau ~5-25s; service_identity.commit == 1691f7f (== pin).

## Probe-harness corrections (documented, product unchanged)

1. MSYS/Git-Bash path mangling: `-e SCP_PC_WORKING_DIR=/var/lib/scp` bị shell
   tự chuyển thành `C:/Program Files/Git/var/lib/scp` → container crash
   `PermissionError: /app/C:`. Fixed bằng MSYS_NO_PATHCONV=1 khi chạy docker.
   Lần 2: `/var/lib` root-owned trong khi image chạy uid 10001 → chuyển sang
   các dir writable của image: /app/data + /app.
2. Probe-script lần đầu crash do HARNESS truncation 120 ký tự làm JSON cụt
   (JSONDecodeError) — sửa probe script, product untouched, chạy lại toàn bộ.

## Probe matrix (results in D3-m10-runtime.json; ALL PASS, 12/12)

Negative-first — auth-first fail-closed (BFLA):
- N1 POST /v105/ask/stream  no auth            -> 401 "Missing auth token"
- N2 POST (Bearer wrong-token-1)               -> 401 "Invalid auth token"
- N3 POST (Basic scheme)                       -> 401 "Invalid auth token"
- N4 POST (Bearer wrong-token-2)               -> 401 "Invalid auth token"
- N5 POST (Bearer wrong-token-3)               -> 401 "Invalid auth token"

Adversarial observation (expected, disclosed): 5 failed attempts tripped
verify_admin's rate limiter:
- N6 6th failed attempt                        -> 429 lockout (designed fail-closed)
- N7 VALID admin token during lockout          -> 429 (limiter does not leak validity)
- Window self-recovered after 65s wait (no restart).

Positive (probe-local admin token, egress deny => offline fail-closed):
- P1 REAL streaming golden path (httpx stream): 200,
  content-type "text/event-stream; charset=utf-8", 6 SSE frames with
  INCREMENTAL arrival timestamps — data physically flows over chunked HTTP in
  two observable waves (classify frames, then judge-done + final); final frame
  step=final/status=complete with verdict field.
- P2 consecutive stream -> 200, 6 frames, verdict FAIL.
- P3 {} with auth                -> 422 (missing question).
- P4 question 5001 chars         -> 422 (max_length=5000).
- L1-L3 parallel burst: 2 concurrent streams (200/6 frames each) + /health
  (200) — no interleave corruption, no 5xx.

## Log scan (probe container, before teardown, lan 2)

- grep -c  "Traceback"            -> 3 — ALL are the AutoFix engine (M7 domain)
  attempting to self-patch BareExceptPass bugs in /app/scp/core/*.py inside a
  read-only container filesystem -> PermissionError -> fail-closed with
  rollback errors logged. Pre-existing (M6 D3 noted the same family). ZERO
  tracebacks reference stream_routes or the ask/stream path (grep
  "stream_routes" trong log = 0).
- grep -ci "database is locked"   -> 0
- grep -ci "bypass"               -> 0
- grep -c  "RATE-LIMIT"           -> 2+ (designed N1-N5 lockout accounting)

## Teardown

- docker rm -f scp-m10-probe -> done. Probe token file removed.
- Main deployment scp-scp-api-1 left RUNNING at the pin (health 200 after
  teardown).

## Evidence limits

- The main deployment runs profile=core where the stream group is NOT
  registered (404) — same known gap as M2/M3/M4/M6; runtime proof for the
  stream route is valid on the full-profile pinned instance above.
- step:error fault-injection (judge raising mid-stream) is not reachable
  without mocking the judge; the branch is audited at code level (D6) and
  recorded as a known gap in the closure record.
