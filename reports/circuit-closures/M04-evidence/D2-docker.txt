# M04 D2 — Runtime proof at pin e13fad455afbfaa7b5e53ec677c1e9772eecc19d

Date: 2026-09-11. Branch: audit/runtime-guard-AUDIT-20260909. Worker: MC4 (attempt 3).

## Commands + results

- Build attempt 1: `SCP_GIT_SHA=0d13c85... docker compose build scp-api` -> EXIT=1 —
  TRANSIENT egress failure (buildkit metadata fetch: TLS handshake timeout to
  auth.docker.io for python:3.12-slim; base image was not present locally). Not a
  product defect. Retry 1 (per DNA #12 small reversible step): EXIT=0.
- Final build: `SCP_GIT_SHA=e13fad455afbfaa7b5e53ec677c1e9772eecc19d docker compose build scp-api`
  -> EXIT=0 (log: D2-docker-build.txt). Image scp-api:local =
  sha256:0f8246a466a3183f0310969481ffcac1285060e39c0d5f58bed3503f360f6c15
- `docker compose up -d --force-recreate scp-api` -> container scp-scp-api-1
  Recreated/Started. RestartCount=0, StartedAt=2026-09-11T07:10:03.849457766Z,
  Status=running.
- GET http://127.0.0.1:8000/health -> HTTP 200
  {"status":"ok", "service_identity":{"commit":"e13fad455afbfaa7b5e53ec677c1e9772eecc19d", ...}}
  -> service identity commit == sha_pin TRUE.
- GET http://127.0.0.1:8000/readiness -> HTTP 200
  {"status":"ready","service":"scp-api","version":"14.0.0",
   "checks":{"judge":"ok","background_scheduler":"ok"},"reason":null}

## Pin lineage inside this session

- 0d13c85 fix(M4): bridge fail-closed ordering (product) + D1 test contract rewrites.
  Built + health-verified at this SHA first (commit==0d13c85 observed on /health).
- e13fad4 fix(M4): /v3/hands/execute maps bridge PEP rejection (PermissionError /
  InvalidTokenSignatureError) to HTTP 403 instead of leaking an unhandled 500;
  new HTTP-boundary test [HANDS-6]. D1 suites re-run green at this SHA
  (flow_04 58/58 + regression 18/18, exit 0) BEFORE the rebuild, then image
  rebuilt + recreated at e13fad4. sha_pin for M04-closure.json = e13fad4
  (last product commit of the circuit).

## Profile posture on the main deployment (observed, disclosed)

The main deployment keeps its .env profile (SCP_API_PROFILE=<set, not printed>).
Route gating per scp/api/route_profile.py: "pc_controller" and "web_control"
require profile "full"; "hands" requires "standard". All M4 HTTP route probes
(403 token-only fail-closed + positive mint-token paths) were therefore executed
on a TEMPORARY full-profile instance from the SAME pinned image — see
D3-runtime-setup.txt + D3-m4-runtime.json. This mirrors the M02/M03 procedure.

## Posture notes

- SCP_PC_CONTROLLER_TOKEN is UNSET on the main deployment: every /v3/pc|hands|web
  request fails closed at the token guard (403) — the token-only contract holds
  by default (no shared secret, no bypass).
- SCP_CAPABILITY_SECRET, SCP_API_PROFILE, SCP_EGRESS_MODE are set on the main
  deployment (names only; values never read or printed).
