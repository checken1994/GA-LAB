# M05 D3 — Runtime setup + probe record (pin 6cdb424)

Date: 2026-09-11. Branch: audit/runtime-guard-AUDIT-20260909. Worker: MC-GREEN.

## Instance

- Temporary container `scp-m5-probe`, image scp-api:local
  (sha256:0390f8ebdd2e510909c86da23a614c6ffba5e54664b090f5e8fac5b02dba1242 — the SAME
  pinned image verified in D2), port 127.0.0.1:8006, SCP_API_PROFILE=full
  (override; group "agent" minimum profile standard, "call" minimum "full" per
  scp/api/route_profile.py), torn down after probes (container + isolated volume).
- Base env: `--env-file .env` (same required-var set as compose deployment,
  startup gate left ENABLED — no bypass), then explicit overrides after the env
  file: SCP_API_PROFILE=full, probe-local SCP_AUTH_TOKEN_SECRET + 
  SCP_PC_CONTROLLER_TOKEN (random 32-byte hex each, generated per session, NEVER
  printed or persisted), SCP_MODE=isolated, SCP_EGRESS_MODE=deny,
  SCP_PRODUCTION_MODE=0, SCP_FORCE_HTTPS=0, SCP_HOST=0.0.0.0, SCP_PORT=8000,
  SCP_DATA_DIR=/var/lib/scp/data, SCP_PC_WORKING_DIR=/var/lib/scp,
  SCP_KERNEL_DB_PATH=/var/lib/scp/data/task_kernel.sqlite3.
- Isolated named volume `scp-m5-data` mounted at /var/lib/scp and /app/data so the
  probe (uid 10001) can write; NOT shared with the main deployment volume.
- **Probe-harness corrections (NOT product changes):**
  1. First attempt crashed at PCController init: `PermissionError: '/app/C:'` —
     Git Bash MSYS path conversion rewrote `-e SCP_PC_WORKING_DIR=/var/lib/scp`
     into a Windows path when invoking docker.exe. Fix: `MSYS_NO_PATHCONV=1`.
  2. Second attempt: `PermissionError: '/var/lib/scp'` — image runs as uid 10001
     (non-root) and the path does not exist; raw `docker run` (unlike compose)
     mounts no writable volume there. Fix: isolated named volume (see above).
  3. `docker run` carries the port argument explicitly (`scp-api:local 8000`);
     raw image CMD prints usage without it (same correction as M06 D3).
- Startup: /health 200 after first poll (~15s).

## D2 — main deployment at pin (same session)

- `SCP_GIT_SHA=6cdb4244f0e22354d824a8ae0a7968bb0c8921c9 docker compose build scp-api`
  -> EXIT=0 (log: D2-docker-build.txt).
- `docker compose up -d --force-recreate scp-api` -> scp-scp-api-1 recreated.
- GET http://127.0.0.1:8000/health -> 200, service_identity.commit ==
  6cdb4244f0e22354d824a8ae0a7968bb0c8921c9 (== sha_pin).
- GET /readiness -> 200 {"status":"ready", checks: judge ok, background_scheduler ok}.

## Probe matrix (results in D3-m5-probe.txt; 8/8 PASS)

Negative-first (auth-first fail-closed):
- N1 POST /v3/call/sessions          no auth          -> 401 (verify_admin)
- N2 GET  /v3/call/status            no auth          -> 401 (verify_admin)
- N3 POST /v3/agent/autofix/apply    no auth          -> 401 (verify_admin dependency runs before handler)
- N4 GET  /v3/agent/status           no internal/PC token -> 403 (agent _guard, different failure mode)
Golden path (token via `docker exec printenv`, never printed):
- G1 GET  /v3/agent/status  +X-SCP-PC-Token -> 200 success:true orchestrator:online + statePath + run_id
- G2 POST /v3/agent/plan    +X-SCP-PC-Token {"goal":...} -> 200 status PLAN_READY + agent_run_id + trace_id
- G3 POST /v3/call/sessions +Bearer admin +X-SCP-PC-Token -> 200 success:true call_id + short-lived join
  token (600s, single-call scope by design; token value REDACTED from evidence file)
- G4 POST /v3/agent/run     +X-SCP-PC-Token dry-run=true level=0 -> 200 status PLAN_READY

Rate-limit accounting: 3 failed verify_admin attempts (N1-N3) < 5/60s lockout; golden
probes used correct tokens (no failures recorded).

## Log scan + teardown

- `docker logs scp-m5-probe`: 0 "database is locked", 0 Traceback, no bypass lines
  (tail 400 lines saved: D3-container-log-tail.txt).
- Teardown: container removed, isolated volume `scp-m5-data` removed. Main
  deployment left running at the pin.
