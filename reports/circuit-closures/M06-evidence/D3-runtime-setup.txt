# M06 D3 — Runtime setup + probe record (pin 75e994f)

Date: 2026-09-11. Branch: audit/runtime-guard-AUDIT-20260909. Worker: MC6.

## Instance

- Temporary container `scp-m6-probe`, image scp-api:local
  (sha256:70dce4efcc867fe0a1c3b796d537c9c3609e1c55d2902d1c964f78581f25945d — the SAME
  pinned image verified in D2), port 127.0.0.1:8004, SCP_API_PROFILE=full
  (override; the pinned image itself carries no profile; the "prediction" route
  group requires profile "full" per scp/api/route_profile.py), torn down after
  probes.
- Base env: `--env-file .env` (same required-var set as the compose deployment,
  startup gate left ENABLED — no bypass), then explicit overrides after the env
  file: SCP_API_PROFILE=full, probe-local SCP_AUTH_TOKEN_SECRET (random 32-byte
  hex, generated per session, NEVER printed or persisted), SCP_MODE=isolated,
  SCP_EGRESS_MODE=deny, SCP_PRODUCTION_MODE=0, SCP_FORCE_HTTPS=0, SCP_HOST=0.0.0.0,
  SCP_PORT=8000, SCP_DATA_DIR=/var/lib/scp/data, SCP_PC_WORKING_DIR=/var/lib/scp.
- Probe-harness correction: first `docker run` omitted the image's port argument
  (compose carries `command: ["8000"]`; the raw image CMD prints usage and exits)
  — restarted with `... scp-api:local 8000`. Product unchanged.
- Startup: /health 200 after 2 polls (~6s).

## D2 — main deployment at pin (same session)

- `SCP_GIT_SHA=75e994ff576faee5080aec3f541aa422b7d4ad0a docker compose build scp-api`
  -> EXIT=0 (log: D2-docker-build.txt). Image scp-api:local =
  sha256:70dce4efcc867fe0a1c3b796d537c9c3609e1c55d2902d1c964f78581f25945d
- `docker compose up -d --force-recreate scp-api` -> scp-scp-api-1 recreated.
- GET http://127.0.0.1:8000/health -> 200, service_identity.commit ==
  75e994ff576faee5080aec3f541aa422b7d4ad0a (== sha_pin).
- GET /readiness -> 200 {"status":"ready", checks: judge ok, background_scheduler ok}.

## Probe matrix (results in D3-m6-runtime.json; ALL PASS)

Negative-first — admin-gated evidence (no auth -> 401, BFLA fail-closed):
- N1 POST /v105/predictions/run-cycle  no auth -> 401
- N2 GET  /v105/predictions/pending    no auth -> 401
- N3 GET  /v105/predictions/all        no auth -> 401
- N4 POST /v105/predictions/verify     no auth -> 401
- N5 GET  /v105/predictions/stats      no auth -> 401

Adversarial observation (expected, disclosed): the 5 failed attempts from the
same client IP tripped verify_admin's rate limiter (5 failures / 60s -> 429).
Every subsequent request (positive or negative) answered 429 until the window
expired — the limiter recovered by itself after ~65s without restart. This is
the designed fail-closed behavior, recorded as evidence, not an incident.

Positive (probe-local admin token, egress deny => offline fail-closed crawl):
- P1 POST /run-cycle  -> 200 {"status":"ok","cycle":{total_predictions..error_types}},
     run_id/trace_id present, run_status SUCCESS, ledger_status OK
- P2 GET  /pending    -> 200
- P3 GET  /all        -> 200
- P4 GET  /stats      -> 200
- P5 POST /verify {"limit":20} -> 200 (BEFORE the M6 fix this endpoint always
     answered 500: Verifier.verify_pending() did not accept the documented limit)
- P6 POST /verify {"limit":0}  -> 422 (schema rejection, ge=1)
- P7/P8: two CONSECUTIVE POST /run-cycle -> 200/200 (SQLite-lock check, part 1)
- L1..L5 PARALLEL burst: 2 concurrent run-cycles + stats + all + health -> all 200
  (SQLite-lock check, part 2)

## Log scan (probe container, before teardown)

- grep -ci "database is locked"      -> 0 (also 0 on the recreated main deployment)
- grep -c  "Traceback"               -> 0
- grep -ci "bypass"                  -> 0
- grep -c  "RATE-LIMIT"              -> 12 (expected: the N1-N5 failed attempts)
- grep -c  "Prediction cycle failed" -> 0
- Observation (out of M6 scope, pre-existing): AutoFix [OPT-26] patch-validation
  warnings for scp/core/request_run_ledger.py BareExceptPass — M7/autofix domain.

## Teardown

- docker rm -f scp-m6-probe -> done. Anonymous volumes destroyed with the
  container. Probe token file removed. Main deployment left RUNNING at the pin
  (health 200 after teardown).

## Evidence limits

- Runtime write-concurrency on the predictions TABLE itself is not directly
  exercisable via HTTP (no create-prediction endpoint); the parallel/consecutive
  probes prove read+orchestrate concurrency without lock errors, while
  concurrent WRITES are proven at process level (D1 [PRED-7]: 8 threads x 4
  real Predictor writes, zero losses).
- Crawl phase at runtime ran with SCP_EGRESS_MODE=deny (offline fail-closed);
  live-internet crawling is intentionally not exercised by this probe.
