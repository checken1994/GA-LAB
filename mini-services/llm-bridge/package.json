{
  "name": "scp-llm-bridge",
  "version": "1.2.0",
  "description": "Ollama-compatible SCP bridge with P0 zero-cost inference PEP.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot zero_cost_bootstrap.ts",
    "start": "bun zero_cost_bootstrap.ts",
    "build": "bun build zero_cost_bootstrap.ts --target=bun --outdir dist"
  },
  "dependencies": {},
  "engines": {
    "bun": ">=1.3.0"
  }
}
