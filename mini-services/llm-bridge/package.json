{
  "name": "scp-llm-bridge",
  "version": "1.1.0",
  "description": "Ollama-compatible HTTP bridge that forwards chat/generate requests to OpenRouter. Listens on port 11434 so SCP's default OLLAMA_HOST config works without changes. [R14-BRIDGE] Switched from z-ai-web-dev-sdk (internal-api.z.ai) to OpenRouter direct (public API).",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot index.ts",
    "start": "bun index.ts"
  },
  "dependencies": {},
  "engines": {
    "bun": ">=1.3.0"
  }
}
